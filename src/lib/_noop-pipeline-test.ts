// Scratch marker for the no-op pipeline validation spec (noop-pipeline-test-1).
// Read by nothing; imported by nothing; changes no behavior. Do NOT wire this anywhere.
export const NOOP_PIPELINE_TEST_MARKER = "noop-pipeline-test-1" as const;
