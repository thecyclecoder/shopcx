// core/analytics.js — Stub (GA4 removed during Preact rewrite)
// Portal events are tracked server-side via customer_events table.
// If you need client-side analytics later, add them here.

export function send() {}
export function setPage() {}
export function portalAction() {}
