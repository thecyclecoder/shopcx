# Lifecycle: Spec-and-goal branch PM flow + atomic promotion

The end-to-end trace of how a feature moves **authored spec → built on a branch → tested on a preview → promoted to production**, under the **branch-accumulation** model. The build pipeline never puts a half-built spec or a partial goal on `main`: every phase accumulates on a per-spec build branch, the whole spec is tested on its branch preview before it leaves the branch, and a multi-spec goal lands on `main` in **one atomic merge** when (and only when) the whole goal is done and green.

This supersedes the old **"one PR ships one phase, a phase is `shipped` the instant its PR merges to `main`"** model. That model created a deadlock — phase N's build needs phase N-1's code on `main`, but you can't put phase N-1 on `main` before it's tested — and it let production receive a partial spec (phases 1–2 live, 3 still in flight) or a partial goal (spec A live, spec B still building). Branch-accumulation resolves both: code is tested **before** promotion, and promotion is **atomic** per shippable unit (a one-off spec, or a whole goal).

Design + milestones: [[../specs/spec-goal-branch-pm-flow]]. Hierarchy + status semantics: [[../project-management]]. The build console + box worker that drive it: [[roadmap-build-console]].

## The three branch tiers

```
main (production)
  ▲ ATOMIC — one merge per shippable unit (a one-off spec, or a whole goal)
  │
  ├── claude/build-{slug}   ← the SPEC BRANCH: every phase of a spec commits here,
  │     • P1 → P2 → P3 …      one commit(-set) per phase, each built on the prior phase's tip
  │     • the ONLY prefix that gets a Vercel preview (vercel-skip-non-spec-build-refs)
  │     • pre-merge spec-test + security run against that preview
  │     • a ONE-OFF spec (no goal) promotes from here straight to main  ── Gate A ──▶ main
  │
  └── goal/{goal-slug}      ← the GOAL BRANCH: a goal's finished spec branches merge here,
        • one REAL merge commit per spec, in blocked_by (topological) order  ◀── Gate B
        • seeded from origin/main by the goal's FIRST spec
        • accumulates the whole goal; never touches main until the goal is complete
        • when ALL the goal's specs are on it + green  ── Gate C ──▶ main (one atomic merge)
```

- **Spec branch `claude/build-{slug}`** — every phase of a spec builds here as **one commit(-set) per phase**, each phase building on the previous phase's tip (no `main` round-trip between phases). This is the only branch prefix that gets a Vercel preview (`vercel-skip-non-spec-build-refs` whitelists `^claude/build-`; [[../integrations/vercel]], [[../libraries/vercel-project]]). "Phase built" derives from the phase's commit on this branch (`spec_phases.build_sha`) plus the spec-test verdict — **not** a merge to `main`.
- **Goal branch `goal/{goal-slug}`** — seeded from `origin/main` the **first time any of the goal's specs builds** — **race-safely**: a goal-bound build that finds the goal branch absent creates it with `git push origin origin/main:refs/heads/goal/{slug}` (create-from-main, no checkout) and treats "already exists / non-fast-forward / rejected" as **success** (the concurrent first-build won the create — re-probe confirms the branch is present). So two parallel first-builds coordinate: exactly one creates it, the other observes it; **neither bases on (or PRs to) main**. A goal-bound spec's fresh branch then bases on `origin/goal/{slug}` (sees merged dependencies), and its accumulation-complete PR **targets `goal/{slug}`, never `main`**. Each `in_testing`-eligible, goal-bound spec branch merges in (Gate B) as **one real merge commit** (not a squash — so the goal branch carries each spec's full history for the atomic main promotion), sequenced by `blocked_by`. The goal branch only accumulates; it never pushes to `main` until the goal is complete. (Gate B's `mergeSpecBranchIntoGoalBranch` ALSO seeds the goal branch idempotently on promote — the two seeders are mutually idempotent: same `refs/heads/goal/{slug}` from `main`, "already exists" is success on both sides.)
- **Main / production** — atomic. A **one-off spec** (no goal) merges its spec branch straight to `main` when done + green. A **goal** merges its goal branch to `main` **once**, when every member spec is on it and green — one promotion = the whole goal.

## The three promotion gates

| Gate | What | Driver |
|---|---|---|
| **Gate A** — one-off spec → main | A spec with **no goal** auto-merges its `claude/build-{slug}` branch straight to `main` when promote-eligible. **Guarded to one-off specs only** — a goal-bound branch is HANDED OFF (it must go through Gate B/C, never jump to main here, else it'd ship outside the atomic promotion AND double-stamp). | [[../libraries/github-pr-resolve]] `autoMergeReadyPrs` (goal-bound guard via [[../libraries/agent-jobs]] `resolveGoalSlugForSpec`) → `applyMergedBuildEffects` |
| **Gate B** — spec → goal branch | A goal-bound, promote-eligible spec merges its branch into `goal/{goal-slug}` (created race-safely from `main` at first build OR seeded idempotently here — whichever runs first), **sequenced by `blocked_by`** (Kahn topo-sort). Stamps `specs.goal_branch_sha`. Conflicts surface (`409`), never silently drop. Does NOT push to main. The goal-bound spec's PR already TARGETS `goal/{slug}` (the build path opens it there), so this real-merge integration auto-closes that PR when it lands. | [[../libraries/agent-jobs]] `promoteEligibleSpecsToGoalBranch` → [[../libraries/github-pr-resolve]] `mergeSpecBranchIntoGoalBranch` |
| **Gate C** — atomic goal → main | When every member spec is on the goal branch and green, merge `goal/{goal-slug}` → `main` in **one atomic merge**. Stamps every member phase `shipped`. Respects the parent-goal exemption. | [[../libraries/agent-jobs]] `promoteCompleteGoalsToMain` → [[../libraries/github-pr-resolve]] `mergeGoalBranchIntoMain` → `applyGoalPromotionEffects` |

> **Gate-letter reuse — don't conflate with the auto-ship pipeline's gates.** The auto-ship pipeline ([[roadmap-build-console]] § auto-ship pipeline) also labels its auto-merge-a-ready-PR gate "Gate A" and its auto-fold-a-verified-spec gate "Gate B" ([[../libraries/spec-test-runs]] `autoFoldVerifiedSpecs`). **Gate A is the SAME mechanism in both** — `autoMergeReadyPrs` — it just now carries the goal-bound guard so it only fires for one-off specs. The auto-ship "Gate B (auto-fold)" is a *post-ship* step (shipped → folded) and is unrelated to this flow's Gate B (spec → goal branch). Below, the goal-branch and atomic-promotion gates are always named explicitly (Gate B / Gate C).

## Status flow

```
planned → in_testing → shipped → folded
```

| Status | Means | Where it derives from |
|---|---|---|
| **planned** | Authored + Vale-reviewed, not yet built (or mid-build, no green branch preview) | every phase `planned`/`in_progress`; no full green branch preview yet |
| **in_testing** | **Built + tested on a branch, NOT in production** — every phase carries a `build_sha`, the branch preview's spec-test + security are green | [[../libraries/agent-jobs]] `isSpecPromoteEligible` — accumulation-complete ∧ spec-test-green ∧ security-green (the SAME predicate the board's `applyInTestingOverlay` and the Gate A inline gate read) |
| **shipped** | Promoted to `main` — a one-off spec via Gate A, or a goal-member via the atomic Gate C. Live in prod. | a `main` promotion stamped the phases `shipped` (`applyMergedBuildEffects` one-off / `applyGoalPromotionEffects` goal) |
| **folded** | Post-ship machine spec-test passed → knowledge extracted into permanent brain pages, [[../tables/specs]] row flipped `status='folded'` (preserved) | [[../libraries/spec-test-runs]] `autoFoldVerifiedSpecs` (the auto-ship pipeline's auto-fold gate — see [[roadmap-build-console]]) |

`in_testing` is the load-bearing new state: a spec sits here while it is **complete and green on a branch but not yet on `main`**. It is **derived** ([[../libraries/brain-roadmap]] `SpecStatus`, a slot between `in_progress` and `shipped`), never stored as a phase status — no phase is ever `in_testing`. A built-on-branch phase reads `in_progress` (it has a `build_sha` but no `merge_sha`/`pr` and no `shipped`).

## End-to-end trace

### 1. Author → in_review → Vale → planned

- A feature is authored to `public.specs` + `public.spec_phases` (the authoring chat / Sage / the planner — see [[roadmap-build-console]] § Author). A newly-authored spec lands `in_review`; the build pipeline refuses to dispatch it.
- **Vale** (the [[../specs/spec-review-agent|spec-review agent]], box `spec-review` job) reviews it against the authoring checklist and stamps the durable `specs.vale_pass=true` (`markSpecCardValePassed`). Quality-only — the planned/deferred disposition belongs to Ada.
- Once authored + Vale-passed + every `blocked_by` cleared, the spec is `planned` and eligible to build. The **claim-time build gate** ([[../specs/claim-time-build-gate]], `evaluateClaimTimeBuildGate`) enforces all three at the box's first claim, reading DERIVED status off the brain-roadmap rollup. **Goal-aware blocker clearance** ([[../libraries/agent-jobs]] `resolveGoalSlugForSpec` / `areSpecsGoalMates`): a **goal-mate** blocker clears when it is **on the goal branch** (`isSpecOnGoalBranch` — a goal-mate never ships to main until Gate C), an **external** blocker clears only when **shipped**. This is the fix that stops a goal-mate dependent deadlocking forever.

### 2. Build phases accumulate on the spec branch (one commit per phase)

- The box's `runBuildJob` builds **exactly one phase per session** — the next `planned` phase — and commits it onto `claude/build-{slug}`. A **one-off** spec's fresh branch is created from `main`. A **goal-bound** spec's fresh branch ALWAYS bases on `origin/goal/{goal-slug}` (so it builds on goal-mates already landed) — and if that goal branch is absent, the build **creates it race-safely from `main` first** (`git push origin origin/main:refs/heads/goal/{slug}`; "already exists" = a concurrent first-build won, treated as success on re-probe). A goal-bound spec **never bases on `main`** — that was the regressed race (two parallel first-builds both basing on main → both PR to main → goal branch never created; see Status / open work). Phase N builds on phase N-1's tip; there is **no per-phase PR to `main`** and **no `main` round-trip** between phases.
- Each phase's commit is stamped via [[../libraries/specs-table]] `stampPhaseBuilt(workspace, slug, position, { build_sha })` — recording `spec_phases.build_sha` and moving the phase to `in_progress` (built — NOT shipped). `build_sha` set with `merge_sha`/`pr` null = built-on-branch; it's the "this phase is done building" signal the next-phase advance reads (NOT the main-merge `pr` tag). `build_sha` is the branch-flow replacement for the old per-phase-to-main provenance.
- "Build all" chains: on each phase's commit, the next `planned` phase is queued (built atop the prior phase's code on the same branch) until every phase is built.
- **Phase-push branch-race recovery** ([[../specs/build-worker-rebase-before-push-no-lost-phase-on-branch-race]] · detail in [[../libraries/builder-worker]] § Phase-push recovery). Each phase build (a) fetches the specific `origin/{branch}` ref BEFORE the worktree add, so it always bases on the current remote tip (never a stale/absent `origin/{branch}` after a concurrent push); (b) on a non-fast-forward push rejection, rebases onto the remote tip and retries the push ONCE — a rebase conflict flips to `needs_attention` (never a silent drop of the built commit); and (c) enforces a **single-owner-per-branch invariant** — a slug-scoped stale-sibling reap runs in both `hasActiveBuildForSlug` (the re-enqueue guard) and at build dispatch, transitioning any dead `building`/`claimed`/`queued_resume` row for the slug to `failed` (compare-and-set) so at most one build ever holds `claude/build-{slug}` at a time.
- **The PR is DEFERRED until accumulation completes** (`defer-pr-until-accumulation-complete`). `runBuildJob` routes **both** the normal (new-changes) completion AND the **no-new-changes** completion (a resume that produced no edits) through ONE shared helper — `finalizeBuiltPhase` — so the success path is identically **`stampPhaseBuilt` → `isSpecAccumulationComplete` → (conditional) `ensurePr` → `queueNextChainedPhase`** for fresh and resumed builds alike (`resume-after-approval-stamps-build-sha`). This closes a wedge: a build that paused for `needs_approval` (e.g. a migration phase), got approved, RESUMED, applied the migration, and returned `completed` with **no new file edits** — its phase's code was already committed to the branch during the pause, so the resume's worktree is clean (`!dirty`). The pre-fix `!dirty` branch ran accumulation/PR/chain but **skipped `stampPhaseBuilt`**, so the phase stayed `planned` / `build_sha` NULL forever — accumulation never saw it built, the chain/PR never advanced, and EVERY migration-bearing (or any `needs_approval`-pausing) phase wedged. The `!dirty` path now detects the branch is ahead of `main` (the phase WAS committed during the pause) and hands off to `finalizeBuiltPhase` — which stamps the phase's `build_sha` (idempotently — `stampPhaseBuilt` no-ops a terminal phase and re-stamping the same SHA is harmless; `phasePosition` is derived from the first not-yet-built phase when a resume's instructions named none) before the accumulation/PR/chain logic. Only the genuinely-zero-commits case (Bo found everything already on `main`, nothing committed) falls through to the `reconcileSpecDrift` branch. While a multi-phase spec is still accumulating (a phase still `planned`/un-built), the build **commits + pushes to `claude/build-{slug}` but opens NO PR** — it completes the job on the branch and chains the next phase. `ensurePr` runs ONLY in the build that COMPLETES accumulation (the last phase), so the PR — and thus the board's squash-merge action (already gated on `specStatus === in_testing`) — surface **together** at `in_testing`, never mid-accumulation. The stamp runs **before** the accumulation read so this phase's `build_sha` is already persisted when the gate evaluates. A **one-shot / single-phase spec (0–1 phases)** is trivially accumulation-complete → opens its PR on its single build, exactly as before. The accumulation check **fails OPEN** (a PM-read blip ⇒ treat as complete ⇒ open the PR) so a legitimately-done spec is never wedged PR-less. The branch's OWN Vercel preview (driven by the `^claude/build-` Ignored-Build-Step override, keyed on the git **ref** — not a PR) drives the pre-merge spec-test + security review, so no PR is needed during accumulation. The same accumulation gate guards the **no-new-changes** completion path (a resume that produced no edits won't open a PR mid-accumulation either). The **`needs_input` / `needs_approval` / loop-guard pauses open NO PR — not even a draft** (`no-pr-during-accumulation`): a draft PR mid-accumulation is an unwanted no-op artifact (CEO directive), so a paused build commits + pushes the WIP to `claude/build-{slug}` and stops with **zero PR**. The pause is **PR-independent**: `needs_input` surfaces via the job's `needs_input` status + `questions` payload; `needs_approval` surfaces via the `needs_approval` status + `pending_actions` (the approvals feed + director inbox read those, not a PR), and the approver — Ada (`runDirectorApprovalJob`) or the CEO — inspects the proposed migration SQL / apply script / new code by reading them **off the branch** (the director review checks `spec_branch` out into a read-only worktree; evidence: `noop-pipeline-test-2`'s migration was auto-approved with NO PR open). So no PR (draft or otherwise) exists during accumulation — the real (non-draft) PR opens ONLY at accumulation-complete, exactly like the no-pause path. The **build-recover-pr-create** path ([[../libraries/roadmap-actions]] `createPrForJob`, BuildButton "Create PR") is unaffected: it keys on the `"branch pushed but PR creation failed"` error, which only the accumulation-COMPLETE branch can now stamp (the deferred branches complete cleanly with no PR + no error), so it still recovers a genuinely-done spec whose `ensurePr` failed. `autoMergeReadyPrs` needs the PR present at promote-eligible time — guaranteed: the PR opens in the same last-phase build, before the preview's spec-test/security (its promote-eligibility inputs) finish.

### 3. Accumulation gate → preview → pre-merge spec-test + security

- **Accumulation gate** — `isSpecAccumulationComplete` ([[../libraries/specs-table]]) is true only when **every** phase carries a `build_sha` (or is terminal). A spec with an unbuilt phase is held. This single predicate is read by THREE seams so they never disagree on "fully built on branch": the Gate A auto-merge accumulation gate, the M3 pre-merge-test trigger, and `isSpecPromoteEligible`.
- The spec branch gets a **per-build Vercel preview** ([[../specs/per-build-vercel-preview-deploys]]; `claude/build-*` is the only whitelisted prefix). When the LAST phase's preview reaches READY (accumulation complete + `agent_jobs.preview_url` set), [[../libraries/agent-jobs]] `maybeEnqueuePreMergeSpecTestOnAccumulation` fires ONE `kind='spec-test'` job carrying `spec_branch=branch` (so the runner reads the branch's spec body, not `main`'s) and the preview origin in `instructions`. Earlier phases' previews land READY too, but accumulation isn't yet complete → no-op.
- **Spec-test** ([[../specs/spec-test-agent|Vera]]) runs the spec's `## Verification` bullets against the branch preview — non-destructive checks only; mutating checks stay scoped to the test workspace (`is_test`). **Security** runs in parallel against the unmerged diff + the preview origin ([[../libraries/security-agent]] `branch` mode). Both green over the whole spec = the branch is promote-ready.

### 4. `in_testing` / `isSpecPromoteEligible`

`isSpecPromoteEligible(workspace, slug, branch)` ([[../libraries/agent-jobs]]) returns eligible iff ALL THREE hold:
1. **accumulation-complete** (`isSpecAccumulationComplete`),
2. **spec-test green on the branch preview** ([[../libraries/spec-test-runs]] `isSpecTestGreenForBranch` — the latest pre-merge run for `(workspace, slug, branch)` is a clean machine pass),
3. **security green on the branch** ([[../libraries/security-agent]] `isSecurityGreenForBranch` — `completedClean`).

These are the SAME three signals the Gate A auto-merge gate enforces inline AND the SAME predicates `applyInTestingOverlay` derives `in_testing` from — so the board, the auto-merge gate, and this helper never disagree on "is the spec done testing?". Fails CLOSED on the green signals (absent run ⇒ not eligible). An eligible spec reads **`in_testing`** and takes ONE of two promotion paths by goal membership.

### 5a. Goal-bound spec → goal branch (Gate B)

`promoteEligibleSpecsToGoalBranch` ([[../libraries/agent-jobs]]) runs the goal's promote-eligible specs in **topological order by `blocked_by`** (`sequencePromoteCandidates`):

- For each `isSpecPromoteEligible`, goal-bound spec not yet on its goal branch (`goal_branch_sha` unset), merge `claude/build-{slug}` → `goal/{goal-slug}` via the GitHub `/merges` API ([[../libraries/github-pr-resolve]] `mergeSpecBranchIntoGoalBranch` — a real merge commit, API-only/no checkout so it runs from the box worker AND the webhook). The goal's first spec **seeds** the goal branch from `origin/main`.
- On success, stamp `specs.goal_branch_sha = mergeSha` ([[../libraries/specs-table]] `stampSpecGoalBranchSha`) — the durable "on the goal branch" marker (writes NEITHER status NOR `merge_sha`; on-the-goal-branch is NOT shipped).
- One merge per spec (idempotent — stamped specs skip). The goal branch is **never** pushed to `main` here. **Conflicts surface** (`conflicts[]` / `409`), never silently dropped — the caller escalates.

### 5b. Goal complete → atomic goal → main (Gate C)

`promoteCompleteGoalsToMain` ([[../libraries/agent-jobs]]) gates each greenlit goal in order:

1. **parent-goal exemption** — skip a parent ([[../libraries/goals-table]] `isGoalParentExempt`: `goals.is_parent` flag OR has child goals OR no buildable specs). A parent (e.g. [[../goals/ceo-mode|CEO mode]]) has no goal branch; its **sub-goals promote independently**, each via its own Gate C. There is no single atomic parent promotion.
2. **goal-complete** — `goalBranchState(goalSlug).allOnGoalBranch` ([[../libraries/specs-table]]): ≥1 spec AND every member spec stamped with a `goal_branch_sha`.
3. **green** (combination-verified without an extra preview deploy) — every member spec individually `isSpecPromoteEligible` on its own branch (already tested); because each dependent built OFF the goal branch, the integrated whole was compiled together, and the clean atomic land is the final combination check.
4. **promote** — `mergeGoalBranchIntoMain(goalSlug)` ([[../libraries/github-pr-resolve]]) merges `goal/{slug}` → `main` in ONE merge (a real merge commit, so `main` carries the goal's full per-spec history) + deletes the spent goal branch. A **`409` conflict** HOLDS the goal (nothing stamped, left for the owner). Then `applyGoalPromotionEffects(workspace, goalSlug, mergeSha)` — the **ONLY shipped-writer for goal-bound specs** — iterates `goalBranchState.specs` and, per member spec: stamps every member phase `shipped` with the single goal→main merge SHA, stamps the spec's **card-level `last_merge_sha`** (`stampSpecMergeProvenance`; the atomic merge has no per-spec PR, so `merged_pr` stays null but `last_merge_sha` carries the SHA — without it the card reads drift-suspect), and routes the now-derived-`shipped` spec through the **same reactive fold** a one-off uses (`reactiveFoldOnGateComplete`). The merge_sha **is** the provenance: a phase shipped with a `merge_sha` (even with `pr=null`) is no longer drift-suspect (`phaseHasProvenance` accepts either stamp), and the board's `applyInTestingOverlay` treats "every non-rejected phase carries a merge_sha" as the `merged` signal — so a goal-bound spec derives `shipped` (not stuck `in_testing`) and becomes fold-eligible.
5. **finalize the goal** — `finalizePromotedGoal(workspace, goalSlug)` retires the goal itself: flips `goals.status` `greenlit → complete` (the explicit lifecycle override via `setGoalStatus` — the board normally *derives* `complete`, but the goal-fold lane's guard reads the *stored* column) and enqueues a `kind='goal-fold'` job (deduped). The goal-fold lane folds the goal's durable knowledge into the permanent brain pages and flips `goals.status='folded'`. Without this a promoted goal lingered as `greenlit` on the active board forever.

### 5c. One-off spec (no goal) → main directly (Gate A)

- A spec with **no goal** (`resolveGoalSlugForSpec` → null) doesn't wait for anyone. On `isSpecPromoteEligible`, `autoMergeReadyPrs` squash-merges its `claude/build-{slug}` branch to `main`. `applyMergedBuildEffects` then stamps the **whole spec** shipped: a non-chain multi-phase build with no named phase = a fully-accumulated branch landing in one merge → stamp EVERY non-terminal phase `shipped` (the accumulation gate guarantees the branch only merged fully-built). A chain build still names its phase and ships phase-by-phase; a one-shot (0 phases) records `merged_pr`/`last_merge_sha` on the row.
- One-off specs keep shipping incrementally — only goal-bound work batches.

### 6. Shipped → folded

- Post-promotion the spec is `shipped` and live. Its machine spec-test (now against `main`) runs; on an `approved` verdict with a clean security pass and no open regression, the auto-fold gate ([[../libraries/spec-test-runs]] `autoFoldVerifiedSpecs`) enqueues a fold-build that extracts the spec's knowledge into the permanent brain pages and flips the [[../tables/specs]] row `status='folded'` (preserved). Human QA is advisory and never gates this. See [[roadmap-build-console]] § fold + [[../project-management]] § Folding.
- A **goal-bound** spec folds the SAME way — the post-M5 finalization (5b step 4) routes it through the identical `reactiveFoldOnGateComplete` lane the moment Gate C lands (it doesn't wait for the daily backstop), and the **goal** retires alongside its specs (`greenlit → complete → folded` via `finalizePromotedGoal` + the goal-fold lane). So a promoted goal + all its specs leave the active board, exactly as a one-off spec does.

## The Reva atomic deploy-watch — escalate, not revert

A promotion to `main` triggers a Vercel production deploy, which opens a Reva deploy-watch ([[../tables/deploy_watches]], [[../libraries/deploy-guardian]]) over the canary window. **Two deploy shapes:**

- **Per-spec** — a `claude/<slug>` build branch squash-merged to `main` (Gate A, one-off specs). On a `regressed` verdict Reva **restores known-good FAST** (`revertDeployMerge` auto-revert of the offending squash) + escalates — reverting one spec is cheap.
- **Atomic** — a `goal/<slug>` branch promoted to `main` in one merge (Gate C, carrying many specs). The watch is opened by `promoteCompleteGoalsToMain` passing `workspaceId` + the goal slug + `isAtomic:true` to `openDeployWatch`, and the row is stamped `deploy_watches.is_atomic=true`. A regression here **ESCALATES, never auto-reverts**: rolling back a whole tested goal on a regression bar tuned for tiny per-phase diffs would be a false-revert of many specs' work, so a **human decides** (revert the goal merge, hotfix-forward, or accept). The promotion was atomic and tested-before-merge; the response to a regression is a human decision, not an automatic un-ship of a whole goal.

Migration `20260730120000_deploy_watches_is_atomic.sql` adds the `is_atomic` column; `openDeployWatch` tolerates the pre-migration schema (a `42703` on the unknown column retries without it).

## M6 subsumes the preview-test-promote-pipeline's board/timeline milestone

The branch-flow's **M6** (`in-testing-status-and-board-for-branch-flow`) **subsumes** the preview-test-promote-pipeline's **M5** (`in-testing-board-and-lifecycle-timeline`): both derive the `in_testing` board status and surface branch state on the roadmap board + the lifecycle timeline. The branch-flow version is canonical — it derives `in_testing` from **spec/goal-branch membership + spec-test green** (not just "preview built but tests pending") and adds the goal-accumulation view. It is built once here, not twice. The board surfaces ([[../libraries/brain-roadmap]] M6):

- **Per-spec timeline** — `SpecCard.onGoalBranch` / `goalBranchSha` feed a `BranchPosition` timeline (built on branch → in testing → on goal branch → promoted to main). A one-off spec leaves them false (it promotes straight to main).
- **Per-goal accumulation** — `GoalCard.accumulation` (`deriveGoalAccumulation`) = `{ onGoalBranch, totalSpecs, allOnGoalBranch, exempt, exemptReason }`. `allOnGoalBranch` ⇒ the **"⬆ ready to promote"** badge; `exempt` ⇒ the parent-goal "sub-goals promote independently" note (mirrors `isGoalParentExempt` from already-loaded rows, no extra DB reads).

The preview-test-promote-pipeline's earlier pieces (per-build previews, spec-test-on-preview, the `in_testing` status slot, promote-on-green) are reused as the substrate.

## Why this resolves the old model's failures

| Old model (per-phase-to-main) | Branch-accumulation model |
|---|---|
| Phase N's build needs phase N-1 on `main`, but N-1 can't go to `main` untested → **deadlock** | Phase N builds on phase N-1's commit on the **same spec branch** — no `main` round-trip |
| A spec is `shipped` the instant its phase PR merges → **partial spec in prod** | A spec is `shipped` only when the **whole** spec promotes (Gate A whole-spec stamp / atomic Gate C) — never partial |
| A goal's specs merge to `main` one at a time → **partial goal in prod** | The goal lands in **one atomic merge** (Gate C) when the whole goal is done + green |
| Phase provenance = `(pr, merge_sha)` of a `main` merge | Phase provenance = `build_sha` on the spec branch (built); `pr`/`merge_sha`/`shipped` reserved for the promotion stamp |
| Ada keys "started/partially-built" on the `pr` tag | Ada keys on `branchBuiltCount` (`build_sha` or shipped) — the `pr` tag is 0 for a whole branch-flow spec until promotion |
| Reva blindly reverts any regression | One-off spec → revert; **atomic goal (`is_atomic`) → escalate, not revert** |

## Status / open work

**Shipped (2026-06-28):** the branch-accumulation flow is implemented + brain-documented end-to-end — M1 phases-commit-to-`claude/build-{slug}` (one commit per phase); M2 `stampPhaseBuilt`/`build_sha` provenance + the accumulation gate ([[../libraries/specs-table]] `isSpecAccumulationComplete`, read by all three seams); M3 the pre-merge spec-test trigger (`maybeEnqueuePreMergeSpecTestOnAccumulation`) + `isSpecPromoteEligible`; M4 spec→goal-branch (Gate B, `promoteEligibleSpecsToGoalBranch` / `mergeSpecBranchIntoGoalBranch`, `goal_branch_sha`, `blocked_by`-sequenced, goal-mate blocker clearance); M5 atomic goal→main (Gate C, `promoteCompleteGoalsToMain` / `mergeGoalBranchIntoMain` / `applyGoalPromotionEffects` the sole goal shipped-writer, parent-goal exemption via `isGoalParentExempt` + `goals.is_parent`); M6 board/timeline surfacing (`SpecCard.onGoalBranch`, `BranchPosition`, `GoalCard.accumulation` / `deriveGoalAccumulation`, the "⬆ ready to promote" badge). The audit aligned all agents: Gate A merges one-off specs ONLY (goal-bound → Gate B/C, no double-stamp), Reva's `is_atomic` watch escalates-not-reverts a whole goal, and Ada's groom/init/fix-escort lanes gate on `branchBuiltCount` (not the pr-tag `provenanceShippedCount`). Migrations: `20260726120000_spec_phases_build_sha.sql`, `20260730120000_deploy_watches_is_atomic.sql` (+ the `goals.is_parent` migration).

**Fix (2026-06-28) — `resume-after-approval-stamps-build-sha`:** a phase that paused for `needs_approval` (a migration / any gated phase), got approved, RESUMED, applied the migration, and completed with **no new file edits** previously stayed `planned` / `build_sha` NULL forever — the `!dirty` completion branch ran accumulation/PR/chain but **skipped `stampPhaseBuilt`**, so accumulation never saw the phase built and the chain/PR never advanced (every migration-bearing phase wedged; observed live on `noop-pipeline-test-3` P2). Fix: factored the shared post-build completion (`stampPhaseBuilt` → `isSpecAccumulationComplete` → conditional `ensurePr` → `queueNextChainedPhase`) into one `finalizeBuiltPhase` helper that **both** the fresh-build path and the no-new-changes resume path reach. The `!dirty` path detects the branch is ahead of `main` (the phase was committed during the pause) and finalizes through it — stamping the phase (idempotently; phase position derived from the first unbuilt phase when a resume named none) so a resumed migration phase now stamps `build_sha` → accumulation completes → the real PR opens → `in_testing`. Genuinely-zero-commit "already on main" builds still route to `reconcileSpecDrift` unchanged.

**Fix (2026-06-29) — goal-branch parallel-creation race (PR-targets-goal-branch + race-safe create):** two goal-bound specs of `noop-goal-test` built in PARALLEL; both first-builds read "goal branch absent → I'm first → basing on main," both opened their own PR (#859, #860) with `baseRefName=main`, and `goal/noop-goal-test` was **NEVER created** — so each goal-bound spec shipped its own PR to main instead of accumulating onto the goal branch + landing atomically (Gate C was unreachable). Root cause: TWO bugs compounded. (1) The build-base resolution ([[roadmap-build-console]] `runBuildJob`) had a "first spec of the goal — basing on main" fallback when the goal branch was absent — a read-then-act with no create, so N parallel first-builds all "won" and all based on main. (2) `ensurePr` hardcoded `base: "main"` for EVERY spec, so even a goal-bound spec's PR targeted main. (Gate A's goal-bound guard correctly refused to MERGE those PRs to main — but they were the wrong artifact and the goal never accumulated.) Fix — four parts: **(1)** a goal-bound spec's PR/merge targets `goal/{goalSlug}`, never main — `ensurePr` takes a `base` param, and `finalizeBuiltPhase` resolves the goal slug and passes `goal/{slug}` (one-off → main, unchanged). **(2)** race-safe goal-branch creation at build time — when a goal-bound build finds the goal branch absent it `git push origin origin/main:refs/heads/goal/{slug}` and treats already-exists/non-fast-forward/rejected as success (re-probe confirms), then fetches + bases the spec branch on the goal branch; a goal-bound spec NEVER falls back to main (a create failure is FATAL, not a main fallback) — so two parallel first-builds: exactly one creates it, the other observes it, both end up on the goal branch. **(3)** accumulate — unchanged Gate B (`mergeSpecBranchIntoGoalBranch`, the real-merge-commit integration) still runs; the PR-to-goal-branch auto-closes when Gate B lands. **(4)** atomic Gate C (`promoteCompleteGoalsToMain`) was already wired + fires on goal-completeness — the race had only left it *unreachable* (no goal branch); with the branch now created, it promotes the whole goal in one merge. A new predeploy guard `scripts/_check-goal-branch-targeting.ts` (`npm run check:goal-branch-targeting`) statically asserts the build path never bases/PRs a goal-bound spec on main + the race-safe create + the `ensurePr` `base` param exist, so the regression can't silently return.

**Validated end-to-end hands-off (2026-06-29) — `noop-goal-v2`:** the full goal path was confirmed to run with ZERO manual intervention by the `noop-goal-v2` validation goal ("[TEST] Goal-path v2 — clean hands-off validation," Platform-owned, now folded). Two no-op marker specs (`noop-v2-a`, `noop-v2-b`, one per milestone) both built, both ended up on a **race-safely-created** `goal/noop-goal-v2` branch (neither based on main — the parallel-creation race fix above held), accumulated via Gate B in `blocked_by` order, atomically promoted to `main` in one Gate C merge, and the goal folded — with no human nudge/queue/flip anywhere. This is the clean confirmation that the goal-branch race fix works on the REAL goal path, not just the one-off path. The reusable harness for this kind of run is the multi-spec no-op goal pattern in [[../recipes/pipeline-validation-tests]].

**Fix (2026-06-28) — pre-merge tests fire at accumulation-complete (the resume-after-approval follow-on):** the `resume-after-approval-stamps-build-sha` fix above made the resumed migration phase STAMP + open the PR — but the M3 spec-test + M2 security triggers still never fired on that path, so the spec sat `in_testing` forever waiting for green tests that never ran (`spec_test_runs` EMPTY, no branch-mode security review; observed on `noop-pipeline-test-4` / PR #837). Root cause: BOTH pre-merge triggers fired ONLY from the **success-push** path's `pollCapturePreviewUrl` READY callback. On a resume-after-approval the phase's WIP was committed DURING the `needs_approval` pause (the `commitWip` push opens NO preview poll) and the resume's `!dirty` finalize pushes nothing — so the success-push callback never runs for the accumulation-completing phase and the branch tip doesn't change at finalize (the prior phase's poll doesn't re-fire). Classic event-only trigger with no standing-pass backstop. Two-part fix: **(1) PRIMARY** — `finalizeBuiltPhase`, right after `ensurePr` opens the real PR at accumulation-complete, fires a fire-and-forget `pollCapturePreviewUrl` onto the job row and (on READY) enqueues both `maybeEnqueuePreMerge{SpecTest,Security}OnAccumulation`; **(2) BACKSTOP** — `backstopPreMergeChecks` (the platform-director standing-pass heartbeat) now also recovers a fully-accumulated in-flight spec with an open PR but **no captured preview and no `spec_test_runs` row** — it captures the branch tip's preview ON DEMAND (`capturePreviewUrlForJob`, tightly gated to avoid hammering Vercel), then fires both triggers. Idempotent throughout (capture only advances the row forward; the enqueues dedupe per `(workspace, slug, branch)`). The during-accumulation skip, the Gate A auto-merge BOTH-green gate, and the already-accumulated single-phase preview-ready path are all unchanged. `noop-pipeline-test-4` / #837 self-recovers on the next standing pass via the backstop.

**Fix (2026-06-29) — post-M5 goal finalization (a promoted goal + its specs must finalize, not linger):** `noop-goal-v2` completed the FULL goal path — `main` carried the M4 goal-branch integrations + the M5 `atomic goal promotion: merge goal/noop-goal-v2 into main` commit (the code SHIPPED) — but the DB never finalized: the goal still read `status=greenlit` and the two specs (`noop-v2-a`/`noop-v2-b`) never folded (stored status null, `last_merge_sha` unset, phases shipped with `pr=null`). A one-off spec DOES fold post-merge (its `applyMergedBuildEffects` calls `reactiveFoldOnGateComplete` + `stampSpecMergeProvenance`); a goal-bound spec + its goal did NOT after M5. THREE compounding gaps: **(1)** `applyGoalPromotionEffects` stamped phases `shipped` with the merge_sha but `pr=null` and never stamped card-level `last_merge_sha` for multi-phase specs — so `phaseHasProvenance` (keyed only on `pr`) flagged every goal-promoted phase **drift-suspect**, and the card carried no merge provenance. **(2)** the board's `applyInTestingOverlay` derived `merged` ONLY from the build job's `status='merged'` — but a goal-bound spec promotes via the atomic goal→main merge, so its build job stays `completed`, leaving the spec stuck **`in_testing` forever** (never derived-`shipped` ⇒ never fold-eligible — the deepest root cause). **(3)** `promoteCompleteGoalsToMain` never invoked the fold lane for the specs NOR touched the goal row — so the goal lingered `greenlit` and nothing enqueued a `goal-fold`. Fix — three points: **(1)** `phaseHasProvenance`/`driftSuspectPhases` accept EITHER a `pr` OR a `merge_sha` as the merge-hook stamp (a goal-bound spec lands with no PR; the merge_sha is the actual main commit — a stronger landing proof), and `applyGoalPromotionEffects` now stamps `stampSpecMergeProvenance(slug,{pr:null,merge_sha})` for EVERY member spec. **(2)** `applyInTestingOverlay`'s `merged` signal is true when the build job is `merged` OR every non-rejected phase carries a `merge_sha` (the goal-promoted-to-main case) — so a goal-bound spec derives `shipped` and becomes fold-eligible. **(3)** `applyGoalPromotionEffects` calls `reactiveFoldOnGateComplete` per spec (the SAME fold lane a one-off uses), and `promoteCompleteGoalsToMain` calls the new `finalizePromotedGoal` (greenlit → complete via `setGoalStatus` + enqueue the `goal-fold` lane). This fires for EVERY goal post-M5 in both seams (the box standing pass + the github Gate-C webhook — both call `promoteCompleteGoalsToMain`), so the Growth path retires cleanly. `noop-goal-v2` was finalized via the sanctioned path (specs folded + `last_merge_sha` stamped, goal `folded`) — all off the active board.

**Fix (2026-06-30) — finalize-orphan recovery + standing-pass backstop (`finalize-standing-backstop`):** four genuinely-stuck specs surfaced (the [[../libraries/pipeline-doctor]] CLI) in two classes, and the forensics showed **three of the four wedged on a HEALTHY box** (built hours AFTER the box recovered ~00:23 UTC) — these were LIVE finalize drops, not crash orphans. Root causes + fixes:

- **Class A — built-not-stamped (live no-advance-reconcile wedge).** `fix-growth-allocation-brain-3fd059` (03:05 UTC) + `appstle-attempt-billing-coerce-string-id` (08:30 UTC) both hit `runBuildJob`'s **no-new-changes** branch: Bo found the work already on `main` (appstle's both phases shipped via a SIBLING PR #891; fix-growth's regression already resolved upstream), made zero edits, the branch had no commits ahead of main → `reconcileSpecDrift` ran but returned `status:'in_progress'` with `flipped:[]` (nothing advanced). The handler's success condition `if (r.flipped.length || r.status==='shipped' || r.status==='in_progress')` treated that no-advance reconcile as a **successful completion** — marking the build job `completed` while the phase stayed `in_progress`/`build_sha` NULL/no PR → accumulation never completes, Gate A never fires, the job is terminal so it never re-dispatches: a **permanent SILENT wedge**. Fix: dropped the `|| r.status==='in_progress'` clause — only an ACTUAL advance (`flipped.length || status==='shipped'`) completes the job; a no-advance reconcile now falls through to `needs_attention` (surfaced, never silently completed).
- **Class B — Gate-A green-but-unmerged (conflicting PR + a crashing resolver).** `growth-adopt-storefront-optimizer` (PR #878) + `kpi-audit-…` (PR #847) built green + fully accumulated, but their PRs were **CONFLICTING (dirty vs main)** — `autoMergeReadyPrs` correctly refuses a non-clean PR. The conflict-resolver SHOULD have rebased them, but every `pr-resolve` job for #878/#847 **`failed` with "worktree add failed"** (3× each → retry-cap burned → surfaced needs-human). Root cause: the `runPrResolveJob` worktree-add did `git worktree add -B {branch}` WITHOUT first clearing a stale worktree that already held that branch (a leftover from a build/resolve that crashed during the 5h loop) — the build path guards this via `removeWorktreeForBranch`, pr-resolve didn't. Two fixes: **(1)** pr-resolve now calls `removeWorktreeForBranch(branch)` before the add (`pr-resolve-worktree-add-stale-branch`); **(2)** `enqueuePrResolveJob`'s retry cap now counts only GENUINE resolve attempts — an INFRASTRUCTURE failure (`worktree add failed` / push/fetch/merge died / box crash) no longer burns the 3-strike human-escalation budget (`pr-resolve-cap-ignores-infra-failures`), and the `needs_attention` sentinel is excluded. So once deployed, the standing-pass dirty-PR backstop re-enqueues a fresh, now-succeeding resolve.

**The durable fix — a standing-pass backstop for BOTH legs** (the recurring "event-driven step with no reactive backstop" pattern; `healBuiltUnstampedPhases` was Inngest-only, `detectAndEnqueueDirtyPrs` was webhook-only — both reapable by an Inngest sync / a missed/early webhook): the platform-director standing pass ([[roadmap-build-console]] `runPlatformDirectorStandingPass`) now ALSO runs, each tick, **(A)** `healBuiltUnstampedPhases(workspace)` — stamps a completed/merged build's non-terminal phases `shipped` from the "already merged via #N" / "already built — PR #N implemented …" signal (its regex was widened to catch Bo's `already built — PR #891` wording, the appstle case), idempotent + `healed_built_unstamped` director_activity-logged, and acts ONLY on a strong already-shipped-via-#N proof so a REAL-failure / needs_human spec (which carries no such signal) is never force-shipped; and **(B)** `detectAndEnqueueDirtyPrs(db)` — enqueues a `pr-resolve` for conflicting open `claude/*` PRs (dedup + retry-capped) so the existing standing `autoMergeReadyPrs` merges them clean next pass. **Recovery (sanctioned path, `scripts/_recover-stuck-finalize.ts`):** appstle was healed → both phases `shipped` via #891 (verified: doctor cleared it). storefront-optimizer #878 auto-recovers on deploy (worktree-add + cap fixes + standing backstop). fix-growth + kpi-audit are NOT merged — the evidence (fix-growth has no code to ship; kpi-audit's #847 was deliberately closed as redundant, superseded by #848/folded) makes DEFER the correct disposition, left gated for the CEO (`--defer-obsolete`) rather than force-merged. The two correctly-excluded specs (`growth-allocation-brain` real spec-test FAILURE; `devops-kpi-weekly-snapshot-date-lag-fix` needs_human) were untouched throughout.

**Fix (2026-06-30) — zero-milestone goal silently orphans its specs (`milestone-materialize-at-author`):** `growth-director` (an `is_parent` goal) read **0% / "0 milestones · 0 specs linked"** on the board even though all 12 of its member specs had **folded** (shipped). Root cause — NOT a deletion (the `specs.milestone_id` FK is `ON DELETE SET NULL`, so a milestone delete can never leave a *dangling* ref; it nulls the link cleanly). The milestones were **NEVER CREATED**: `goal_milestones` rows are only written by `upsertGoal` at **propose** time from a `## Decomposition` block ([[../libraries/goal-proposals]] `proposeGoal` → `extractDecompositionMilestones`), and `growth-director` was proposed with a *prose* "Suggested milestone arc" instead — so it seeded **zero** milestones. Pia then decomposed it into M1–M6 specs whose `milestone` handles ("M1"…) + `parent` labels ("M1 — Director core") resolved to **nothing**, and the plan-resume author-time gate ([[roadmap-build-console]] `runPlanJob` RESUME) only flagged an unresolved milestone **when the goal already had ≥1 milestone** (`if (goalForMilestones?.milestones.length && …)`) — so a zero-milestone goal **silently authored every spec with `milestone_id=NULL`**, permanently unlinked → 0% rollup forever. **Two-part durable fix in `runPlanJob` RESUME: (1) materialize-missing-milestones** — before authoring, any milestone an approved spec references that has no row is CREATED from its `M{n}` handle/`parent` label (position parsed, title = the `M{n} — …` label), UNIONed with existing milestones and written via `upsertGoal` (REPLACE-by-position preserves existing ids — never deletes/retitles a prior position, so a partial decomposition can't orphan earlier milestones). The planner's approved decomposition IS the milestone structure. **(2) harden the gate** — dropped the `goalForMilestones?.milestones.length &&` precondition: a spec that NAMES a milestone which still doesn't resolve now fails the job **loudly** (`milestone did not resolve`) instead of authoring an unlinked spec. Box file (`scripts/builder-worker.ts`) — esbuild-parsed; **box restart required** to pick up. **Restore (data, SDK-only `scripts/_restore_growth_director_milestones.ts`):** recreated M1–M6 via `upsertGoal` (preserving the goal row + `is_parent`/`greenlit`) and re-linked all 12 member specs via `attachSpecToMilestone` → growth-director now rolls up **100%** (6 milestones, 12 specs), derived-`complete`, and **stays `greenlit`/active** — a PARENT goal is exempt from atomic promotion and stays on the board awaiting its future Google/Amazon sub-goals (it is NOT folded).

**Fix (2026-06-30) — M4 goal-branch seeding must skip parent + all-shipped goals (`parent-goal-not-goal-branch-promotable`):** a SIDE EFFECT of the milestone restore above — re-linking growth-director's folded specs made `resolveGoalSlugForSpec` return `growth-director` for them, so the M4 lane (`promoteEligibleSpecsToGoalBranch`, the standing-pass "goal-promote" step) picked them up as promote candidates and **spuriously seeded a `goal/growth-director` remote branch** (sha `31d4ed5f`) for a PARENT goal whose specs are all already folded/shipped — nothing to accumulate or promote. Root cause: M4's candidate gate had `goal-bound + promote-eligible + not-already-on-goal-branch` but **no parent-goal exemption and no terminal-spec skip** (unlike `promoteCompleteGoalsToMain`, which already exempts parents). Fix in `promoteEligibleSpecsToGoalBranch` ([[../libraries/agent-jobs]]): after resolving the goal, **(a)** skip any candidate whose goal is `isGoalParentExempt` (parent / has child goals / no buildable specs — memoized per goal), and **(b)** skip any candidate spec already `shipped`/`folded` (it landed one-off on main, bypassing the goal-branch flow) — so an all-terminal goal yields zero candidates and seeds nothing. (`escortApprovedGoals` was NOT the culprit — it's gated to `owner=PLATFORM` + `pct<100`, so a 100% growth-owned parent never enters it.) The spurious `goal/growth-director` branch must be deleted (`git push origin --delete goal/growth-director`) **only AFTER this fix deploys + the box restarts** — until then the box runs the pre-fix code and would re-seed it on the next standing pass.

**Finding (2026-06-30) — non-parent goals that shipped one-off have NO completion path; legacy goals stuck at 100%.** `finalizePromotedGoal` (greenlit→complete + enqueue `goal-fold`) is reachable **only** through `promoteCompleteGoalsToMain`'s atomic goal→main promotion, which requires a `goal/{slug}` branch with `allOnGoalBranch`. Goals whose specs shipped **one-off** (before the goal-branch machinery, or via individual Gate-A merges) have **no goal branch** → `goalBranchState.allOnGoalBranch=false` → they sit `notReady` forever: derived-`complete` (100% rollup) but stored `greenlit`/`proposed`, never folded, never leaving the active board (the Goals page Archive renders `status='folded'` only). Eight such non-parent goals are currently stuck at 100% (`noop-goal-test`, `acquisition-research-engine`, `devops-director`, `grow-surface-platform-agent-team`, `platform-department-scorecard`, `db-driven-specs`, `storefront-optimizer`, `preview-test-promote-pipeline`). **Recommended durable fix (not yet wired):** a standing-pass reconciler that, for each non-parent, 100%-rollup goal with **no member carrying a `goal_branch_sha`** (legacy one-off — nothing to atomically promote), calls `finalizePromotedGoal` (complete → goal-fold). Parents (`is_parent` OR has child goals — e.g. `growth-director`, structurally `ceo-mode`) are excluded: a parent at 100% stays active awaiting sub-goals. A dry-run-verified backfill (`scripts/_backfill_legacy_goal_folds.ts`) is prepared; executing it (a multi-goal archive flip) awaits explicit owner authorization (out of the growth-director-scoped task).

**Substrate reused (already shipped):** per-build Vercel previews scoped to `claude/build-*` ([[../specs/per-build-vercel-preview-deploys]], `vercel-skip-non-spec-build-refs`); pre-merge spec-test + security on the branch preview ([[../specs/spec-test-on-preview-pre-merge]], [[../specs/security-test-on-preview-pre-merge]]); the `in_testing` derived status slot (`in-testing-derived-status`); one-phase-per-build-session; the auto-ship pipeline's auto-merge (the Gate A merge primitive) + auto-fold gates ([[../specs/auto-ship-pipeline]]); the claim-time build gate ([[../specs/claim-time-build-gate]]) + Vale spec-review ([[../specs/spec-review-agent]]).

**Feature (2026-07-06) — Goal-member build serialization (`serialize-goal-member-spec-builds` Phase 1 + 2).** Goal-bound specs of the SAME goal must not build concurrently (concurrent builds race for the same hot files and collide on `action-executor.ts` imports / refund handlers, parking as DIRTY). Phase 1 ([[../libraries/agent-jobs]] `evaluateGoalMemberBuildDispatch` / `decideGoalMemberBuildDispatch`) gates build-claim in `evaluateClaimTimeBuildGate` (leg 4, after blocked_by clearance) — refusing a claim when (a) another goal-mate build is in-flight (claimed/building) OR (b) this spec is not the EARLIEST ready goal-member in `blocked_by`-topological order (Kahn-sorted by slug tiebreak). A non-claimable spec returns `queued` + requeue disposition (never park), so the next platform-director standing pass re-releases it as the prior sibling lands on the goal branch + merges onto `goal/{slug}` (Gate B). One-off specs (no goal) no-op (goalSlug=null) — cross-goal parallelism is preserved. Phase 2 ([[../libraries/github-pr-resolve]] extended `getPr` + [[../libraries/agent-jobs]] `reconcileDirtyGoalMemberPrs` / `decideGoalMemberPrRedrive`) detects DIRTY goal-member PRs (mergeable_state ∈ {dirty, behind}) — the base has advanced past the spec branch — and enqueues a `pr-resolve` job via the standing-pass reconciler (alongside the main-based `detectAndEnqueueDirtyPrs` lane). `runPrResolveJob` now reads `pr.base.ref` dynamically and merges into `origin/{baseRef}` (validated as `main` or `goal/*`; falls back to `main` for safety) — preserving one-off spec behavior (baseRef=main) while handling goal-bound PRs that target `goal/{slug}`. Evidence ([[../specs/serialize-goal-member-spec-builds]]) — git commit b747504c, test suite: 6 dispatch cases (multiple ready goal-mates → exactly one claimable; next becomes claimable after prior merges to goal branch; in-flight + ordering guards; no self-block) + 10 redrive cases (dirty/behind → redrive; read-fail/unknown/clean/unstable/blocked/merged/closed → skip).

**Feature (2026-07-08) — Enqueue-time admission gate for goal-member builds (`goal-member-builds-gate-at-enqueue-not-at-claim` Phase 1).** The `serialize-goal-member-spec-builds` Phase 1 serializer above gates at CLAIM time (leg 4 of `evaluateClaimTimeBuildGate`) — a valid safety net, but it lets N goal-mates land as `queued` rows first, then re-queues the losers each tick. The CEO's board reads as a pile-up until the sibling completes. This spec moves the (b) in-flight leg UP to enqueue: [[../libraries/agent-jobs]] `evaluateGoalMemberEnqueueAdmission` / `decideGoalMemberEnqueueAdmission` sits BEFORE the `agent_jobs` insert in the three build-enqueue chokepoints — `enqueueBuildIfDue` (the reactive gated chokepoint), `queueNextChainedPhase` (the chain-continuation path), and [[../libraries/roadmap-actions|roadmap-actions]] `queueRoadmapBuild` (the owner-tapped Build button + Slack `/build`). If ANY goal-mate row is in `ACTIVE_STATUSES` (queued / queued_resume / claimed / building / needs_input / needs_approval / blocked_on_usage), the enqueue is REFUSED and the spec stays ELIGIBLE-BUT-UNQUEUED — the reactive path re-fires the moment the sibling completes. Fail-OPEN on any resolve error.

**Feature (2026-07-08) — Release-on-completion + retire claim-time serializer (`goal-member-builds-gate-at-enqueue-not-at-claim` Phase 2).** Closes the loop on Phase 1. Phase 1 refused a second goal-mate at enqueue time; Phase 2 admits the NEXT one automatically when the active one merges. [[../libraries/agent-jobs]] `admitNextGoalMemberOnCompletion` / `pickNextGoalMemberCandidates` runs from `applyMergedBuildEffects` (the shared post-merge body — reconcileMergedJobs + handleAutoMergedBuildBranch), ORDERED AFTER `queueNextChainedPhase` so a chain_phases=true spec's next phase queues first + re-occupies the goal's slot; the release then observes it via Phase 1's admission gate and no-ops. When the completed spec has no chain continuation, the release walks the goal's remaining member roster in alphabetical order (matching Phase 1's Kahn tiebreak) and calls `enqueueBuildIfDue` on each — the FIRST admission wins the slot; every subsequent candidate is refused by Phase 1's `evaluateGoalMemberEnqueueAdmission`, so exactly ONE new goal-mate lands per completion. The claim-time serializer at `scripts/builder-worker.ts:5615` / `evaluateGoalMemberBuildDispatch` (leg 4 of `evaluateClaimTimeBuildGate`) is retained as a DEFENSIVE ASSERTION: it now WARN-logs `[phase1-gate-leak]` + requeues with reason `claim-gate defensive assertion (phase1-gate-leak): …` if Phase 1's fail-open path ever admits a second goal-mate — the healthy-run signature is now the absence of both the `[phase1-gate-leak]` log line and the old "held until the goal serializer releases" phrase (that requeue reason is deleted). Evidence — new tests: `src/lib/goal-member-release-on-completion.test.ts` (6 predicate cases: skip completed / shipped-folded / on-goal-branch / empty; deterministic alphabetical order; null-status eligible).

**Known gaps / not yet shipped:** none material to the flow — the M1–M6 chain + the goal-member serialization gates (enqueue-time admission + release-on-completion + claim-time defensive assertion) are in place. M6 subsumes the preview-test-promote-pipeline's M5 board/timeline milestone (intentionally not built twice).

**Open questions:** None.

## Related

[[../specs/spec-goal-branch-pm-flow]] · [[../project-management]] · [[roadmap-build-console]] · [[../tables/spec_phases]] · [[../tables/specs]] · [[../tables/goals]] · [[../tables/deploy_watches]] · [[../libraries/agent-jobs]] · [[../libraries/specs-table]] · [[../libraries/github-pr-resolve]] · [[../libraries/spec-test-runs]] · [[../libraries/security-agent]] · [[../libraries/deploy-guardian]] · [[../libraries/brain-roadmap]] · [[../libraries/platform-director]] · [[../integrations/vercel]] · [[../specs/spec-test-agent]] · [[../specs/spec-review-agent]] · [[../specs/claim-time-build-gate]] · [[../specs/auto-ship-pipeline]] · [[../recipes/build-box-setup]] · [[../recipes/pipeline-validation-tests]]
