# Lifecycle: Roadmap Build Console (self-driving roadmap)

Describe a feature → spec → autonomous build on the **Max subscription** → answer questions / approve prod actions → merge — all from the dashboard (phone-friendly), no laptop or terminal. This is the end-to-end home for the shipped [[../specs/roadmap-build-console]] + [[../specs/build-approval-gates]] specs.

## Two layers (don't conflate)

- **Brain markdown (`main`)** — canonical, *static* spec definitions + final status (the `⏳ planned · 🚧 in progress · ✅ shipped · ❌ cut` phase emojis). Changes only when a PR merges. The board parses it.
- **`agent_jobs` (DB companion)** — *live, actionable* state: build status, `needs_input` questions, `needs_approval` pending actions. The box worker polls it; the dashboard renders it as buttons. Pending actions live here (not on the unmerged branch) — that's why a DB companion is required.

## Two runtimes

- **Vercel app** — the dashboard surfaces + APIs. Never touches the box. A "Build" tap just **inserts an `agent_jobs` row** in Supabase.
- **The box** ([[../recipes/build-box-setup]]) — a Hetzner CCX33, **Tailscale-only inbound**, runs `systemd: shopcx-builder` as the **non-root `builder`** user. It **polls Supabase outbound** (no inbound; the firewall stays shut — the box dials out, the app never dials in), claims a job, and runs the build as a headless `claude -p` on **Max**.

## End-to-end trace

1. **Author.** `/dashboard/roadmap` → **✨ New feature** opens the authoring chat (`POST /api/roadmap/chat`), now **box-hosted on Max** (box-spec-chat — no longer the Anthropic API). Each turn enqueues a `kind='spec-chat'` [[../tables/agent_jobs]] row that resumes a long-running `claude -p` session on the build box, so the model has the **whole working tree** (`Read`/`Grep`/`Glob` over `docs/brain/` + `src/`) and **`WebSearch`** every turn — far more grounded than the old brain-index + two-GitHub-tools call, and **$0 marginal**. The route appends the user turn + sets `turn_status='thinking'`; the box appends the reply; `AuthoringChat.tsx` polls `chat-session?id=` and shows "thinking on the box… (a minute)". Talk it through → **Save spec** (the box resumes the session, emits the full spec markdown, the worker commits `docs/brain/specs/{slug}.md` to `main` via the GitHub Contents API) or **Save & build** (also queues a `build` job). **Refine with Opus** on a spec's detail page passes `slug` → the box Reads the current spec from the working tree as grounding. (Hand-written specs work too.)
2. **Dispatch.** **Build** on a card → `POST /api/roadmap/build` → inserts `agent_jobs` (`queued`). One active build per spec **for a plain Build tap** (no instructions): a second tap coalesces into the live job (`alreadyActive`). Variants: per-phase **build** ([[../dashboard/roadmap]] PhaseList) and **Report issue** both queue a build scoped via `instructions` (no spec edit, spec stays ✅) — and because they carry **new, distinct instructions**, when a build is already live they do **not** coalesce-and-drop; they enqueue a **distinct follow-up `queued` row** (`queuedBehindActive`) that runs after the active build (per-spec serialization). The UI/Slack say "queued to run next" — the issue text is never lost. See [[../specs/fix-report-issue-dropped]].
3. **Claim.** The worker calls `claim_agent_job()` (atomic, `FOR UPDATE SKIP LOCKED`) → `building`. It runs `claude -p --dangerously-skip-permissions` (bypass, no prompts) via the **`build-spec` skill**, as `builder`, with prod-write secrets **stripped from the build env** and no `ANTHROPIC_API_KEY` (stays on Max).
4. **Outcomes** (the build emits one final-status JSON):
   - `completed` → worker runs `npx tsc --noEmit` gate, commits, opens a `claude/*` PR (`ensurePr` retries `gh pr create` with backoff first — most failures are transient).
   - **PR-create failed but branch pushed** ([[../specs/build-recover-pr-create]]) → after the retries are exhausted the worker flags `needs_attention` `error="branch pushed but PR creation failed"`. This is **recoverable** (the build is done + on origin): the card shows **Create PR** (primary) → `POST /api/roadmap/build { jobId, recoverPr:true }` → `createPrForJob` opens/adopts the PR for the pushed branch and flips the job → `completed` — *not* a Rebuild that throws the build away. (Re-validates the #185 case.)
   - `needs_input` (product question) → worker records `questions`, draft PR, pauses; the card shows an **answer form** → `POST /api/roadmap/answer` → `queued_resume` → worker `claude --resume`s.
   - `needs_approval` (gated prod action: `apply_migration` / `run_prod_script` / `merge_pr`) → worker records `pending_actions` + pauses; the card shows **Approve & apply** with the command preview → `POST /api/roadmap/approve` → worker (which holds prod creds) **executes the approved action**, then `--resume`s. The build itself never touches prod (credential-enforced).
   - `failed` / `needs_attention` → surfaced with `error` + `log_tail`. A build that finishes with **no file changes and no PR** is flipped to `needs_attention` carrying the agent's `no_changes_reason` (the build-spec contract emits it when it makes no edits) — never a bare `completed` with no PR that masquerades as done (the card shows Rebuild as a retry). See [[../specs/fix-report-issue-dropped]] Phase 3.
5. **Review + merge.** The card's **Squash & merge** (or [[../dashboard/branches]]) merges the `claude/*` PR — owner-only, server-revalidated.
6. **Status reflects reality.** Spec emojis drive the board columns; `agent_jobs` drives the live per-card build chip + buttons.
7. **Verify → fold → archive.** **Shipped (✅)** = built + deployed (automated). The owner-only, human gate **Verified** comes after: once Dylan confirms a shipped feature works in prod, **Mark verified & archive** (`BuildButton`) queues a **fold-build** (`POST /api/roadmap/build` `{ verify: true }` → canonical fold instructions in the build route, not a spec rebuild). The build folds the spec into its permanent brain homes, appends a one-line entry to [[../archive]], `git rm`s `specs/{slug}.md`, and opens a PR. Merge → the spec leaves the board into the collapsed **Archived** section (reads `archive.md`). The Shipped column is relabeled **"Shipped — awaiting verification"** so it stays a short, real to-do list. Convention: `shipped → verified → fold + delete + archive-index` ([[../project-management]]). Nothing is lost — knowledge in the brain, pointer in the archive, spec `git show`-recoverable.
8. **Re-hydrate (New spec from brain).** Revisiting an archived (or any reference) feature doesn't reactivate the stale spec: **New spec from brain** seeds the authoring chat with the *current* brain page (`POST /api/roadmap/chat` `{ seedSlug }`) and Opus drafts a *fresh* spec to extend/fix it → normal Build flow. The authoring chat in reverse.

## Safety model

Bypass is safe because of four things, not prompts: (1) **PR-gate** — code never hits prod until you merge; (2) **powerless build env** — secrets stripped, so a runaway command can't reach prod (credential-enforced); (3) **gated prod actions need a tap** — `apply_migration`/`run_prod_script`/`merge_pr` only run after owner approval, executed by the worker; (4) **non-root** builder user. The worker is the *only* component with prod creds.

## Billing

- **Authoring chat** → Anthropic API (Opus `claude-opus-4-8`), cheap conversation tokens.
- **Builds** → **Max subscription** (box `claude -p`, no API key). Verified: a real build ran bypass-as-builder on Max and opened a PR (2026-06-18).

## Code map

- Board + detail: `src/app/dashboard/roadmap/{page,[slug]/page}.tsx`; parser `src/lib/brain-roadmap.ts` (`getRoadmap` + `getArchive` for the Archived section).
- Components: `BuildButton.tsx` (build · status · answer · approve · squash-merge · report-issue · **mark verified & archive**), `StatusControl.tsx`, `PhaseList.tsx` (per-phase status + cut + build), `AuthoringChat.tsx` (new + refine + **seed / re-hydrate** + **persist / resume** + **box polling** — each turn enqueues a `spec-chat` job and polls [[../tables/roadmap_chats]] `turn_status` while the box thinks; the thread survives closing the modal + resumes cross-device).
- APIs: `src/app/api/roadmap/{build,status,answer,approve,chat,chat-session}/route.ts` (build accepts `{ verify: true }` → `enqueue_fold` coalesces into one batch fold-build; **chat now ENQUEUES a `spec-chat` box job** — turn / finalize / `{ action:'generate_verification' }` (verify) / `{ seedSlug }` re-hydrate — it no longer calls Anthropic; chat-session is the poll + resume read); merge reuses `/api/branches/[number]/merge`. Archive index: `docs/brain/archive.md` is **generated** from `docs/brain/archive.d/*.md` by `scripts/brain-index.mjs` (`npm run brain:index`).
- Queue: [[../tables/agent_jobs]] + [[../tables/pending_folds]] + `src/lib/agent-jobs.ts` (`getLatestJobsBySlug` · `getPendingFolds`) + `claim_agent_job(p_kinds)` / `enqueue_fold()`.
- Worker: `scripts/builder-worker.ts` (box) — `runJob` / `runPlanJob` / `runFoldJob` / `runProductSeedJob` / `runSpecChatJob`, per-kind lanes (5 build + 1 fold + 2 seed + 1 spec-chat). Box runbook: [[../recipes/build-box-setup]].
- Skills: `.claude/skills/build-spec/` (executes a spec) · `.claude/skills/spec-chat/` (authors one with the founder, box-hosted).

## Status / open work

**Shipped (2026-06-18):** the full loop — authoring chat (new + refine), board with editable status + per-phase status/cut, build dispatch + per-phase build + report-issue fix-builds, the box worker (non-root, bypass, sandboxed, Max), `needs_input` answer loop, `needs_approval` approval gates, and phone-merge. Box hardening (Phase 1 of build-approval-gates) live-proven via a real bypass-as-builder build. The authoring chat is now **brain-grounded** (archived: [[../archive]]): brain index in the system prompt + read_brain_page / grep_repo tool loop, applied to both chat and finalize.

**Awaiting first real exercise:** the `needs_input` and `needs_approval` round-trips are fully wired + deployed but haven't been triggered by a real build yet (the smoke build completed without needing either). The next migration-requiring build (e.g., finishing a stalled spec) will exercise the approval gate live.

**Shipped (2026-06-18, lifecycle + archival):** the **Verified** gate — Mark verified & archive on shipped cards queues a fold-build (`{ verify: true }`) that folds + appends to [[../archive]] + `git rm`s the spec + opens a PR; the Shipped column relabeled "Shipped — awaiting verification"; the collapsed **Archived** section (reads `archive.md`); and **New spec from brain** re-hydration (`AuthoringChat seed` → chat route `{ seedSlug }` seeds Opus with the current brain page). No schema change — reuses `agent_jobs` + the build pipeline.

**Shipped (2026-06-18, fold-build batching):** "Mark verified & archive" no longer spawns one build per spec. `enqueue_fold` ([[../tables/agent_jobs]]) marks the spec [[../tables/pending_folds|pending-fold]] and coalesces into **one** `kind='fold'` job that folds **all** pending-fold specs in a single branch/PR; the worker runs that fold in its own **concurrency-1 lane** (5 lanes stay for builds) so it never races a feature build. The two contended index files are now **generated by a single writer**, never hand-edited by folds: each fold writes only `docs/brain/archive.d/{slug}.md` and **never touches the aggregates**, while a single-writer reconcile — the daily cron [[../inngest/brain-index-refresh]] (`0 9 * * *`) **plus** a `brain/index.refresh` event fired by [[../libraries/roadmap-actions]]`.mergeClaudePr` after every `claude/fold-*` merge — regenerates `archive.md`'s Index + `README.md` folder counts via [[../libraries/brain-index]] (`regenerateBrainIndex`; byte-identical zero-dep copy in `scripts/brain-index.mjs` for local `npm run brain:index`), committing only on a real diff (no-op/no-loop guarded; `next.config.ts` traces `docs/brain/**/*.md` into `/api/inngest`). So two folds never touch the same line. The board shows pending-fold cards as **"Folding…"** (`getPendingFolds`). See [[../specs/fold-build-batching]]. Migration `20260618160000_fold_batching.sql`.

**Shipped (2026-06-19, Slack front-end):** a **second front-end** over this same backend — the [[../integrations/slack-roadmap-console|Slack Roadmap Console]] in `#roadmap`. View the board (`/roadmap`), commission builds (`/build`, `/bug`, Build buttons), answer `needs_input` (modal), approve `needs_approval` (Approve & apply / Decline), and squash-merge — all from a phone. New inbound endpoints `POST /api/slack/{events,interactions}` (HMAC-verified, `SLACK_SIGNING_SECRET`); the owner-gated mutations were extracted to [[../libraries/roadmap-actions]] so dashboard + Slack share one copy of the logic; Slack identity is a UX filter ([[../libraries/slack-identity]]) over the server-revalidated gate. A Vercel cron ([[../inngest/slack-roadmap-notify]]) pushes status transitions back to `#roadmap`, deduped via `agent_jobs.slack_notified_status`. **No new build engine / approval logic; the box stays Slack-unaware.** Migration `20260619120000_agent_jobs_slack_notified_status.sql`.

**Shipped (2026-06-19, chat persistence):** the authoring chat is now **DB-backed** — `AuthoringChat.tsx` autosaves its transcript (debounced ~800 ms, serialized) to [[../tables/roadmap_chats]] via owner-gated `src/app/api/roadmap/chat-session/route.ts` + [[../libraries/roadmap-chats]] (`saveChat`/`loadChat`/`loadActiveChatForSlug`/`listRecentChats`). Closing the modal no longer loses the thread: **Refine** offers Resume-vs-Start-fresh on the latest `active` session for the slug; **New feature** offers a recent-chats picker — both cross-device since the state lives in Postgres. Saving a chat ≠ committing a spec; finalize flips the row `active`→`finalized` and links its `spec_slug`. Migration `20260618140000_roadmap_chats.sql`.

**Shipped (2026-06-19, Slack App Home):** the board is also mirrored onto the Slack **App Home tab** — open the ShopCX app in Slack → see specs grouped In progress / Planned / Shipped with counts + a build-box health header, each a compact one-line row with a single **Details** affordance. Tapping Details opens an in-Slack **modal** (`views.open`) carrying the spec's status · owner · parent · phases · `## Verification` how-to-test steps, with **Build all** / per-phase **Build N** / **Mark verified & archive** living IN the modal — review + build end-to-end with no outbound hop ([[../specs/slack-home-detail]]; "Open in ShopCX" is a footer link, owner-gated; non-owners see the detail but no action buttons). `app_home_opened` → `views.publish` and `roadmap_details:*` → `views.open` via [[../libraries/slack-home]] (`buildHomeView` / `buildSpecModal` / `publishHome`), routed through the same signature-verified `/api/slack/interactions` endpoint → [[../libraries/roadmap-actions]]; the Home view re-publishes (and the modal updates in place) post-queue so status flips immediately. _(An earlier native Slack List mirror — `lists:read/write` scopes, `workspaces.slack_roadmap_list` — was retired by this spec; the App Home is now the single in-Slack destination.)_

**Shipped (2026-06-19, verification guides):** every spec now carries a concrete `## Verification` checklist ("go to X → do Y → expect Z"), authored by the build that ships it ([[../project-management]] § Writing a spec + the `build-spec` skill + the authoring chat's `BASE_SYSTEM`). The detail page renders it as a teal **"✅ How to verify in prod"** card right under **Mark verified & archive** ([[../dashboard/roadmap]], `VerificationCard.tsx`; `extractSpecSection`/`stripSpecSection` in `src/lib/brain-roadmap.ts` lift it out of the article body). A spec lacking one shows an owner-only **Generate test plan** button → `POST /api/roadmap/chat` `{ action:'generate_verification' }` (now a box spec-chat `verify` job — a Max `claude -p` Reads the spec + brain, emits the section, the worker commits it to `specs/{slug}.md`). The shipped backlog was backfilled in one pass.

**Shipped (2026-06-19, worker self-update):** the box worker keeps its own code current — each idle poll tick (only when `active.size === 0`, never mid-build) it `git fetch`es `origin/main`, and if behind `git reset --hard` + `process.exit(0)` so `systemd Restart=always` relaunches the fresh `builder-worker.ts` (`npm ci` first if the lock changed; per-SHA crash-loop guard in `/home/builder/.worker-startup.json`). A merged worker fix is now live within one idle cycle with no manual redeploy. The worker upserts a heartbeat ([[../tables/worker_heartbeats]] — running SHA · active builds · last poll) surfaced as the **Build box** banner on [[../dashboard/branches]]. See [[../recipes/build-box-setup]] § Worker self-update.

**Shipped (2026-06-20, box-hosted spec chat):** the authoring chat moved **off the Anthropic API onto the build box** as a long-running, resumable `claude -p` session on **Max** ($0 marginal). `POST /api/roadmap/chat` no longer calls Anthropic — it appends the user turn to [[../tables/roadmap_chats]], flips `turn_status='thinking'`, and enqueues a `kind='spec-chat'` [[../tables/agent_jobs]] job; the worker's `runSpecChatJob` (own **concurrency-1** lane) resumes the same `box_session_id` in a stable per-chat worktree, so the model has full working-tree `Read`/`Grep`/`Glob` over `docs/brain/` + `src/` and `WebSearch` every turn — strictly better grounding than the old brain-index + two-GitHub-tools call. `AuthoringChat.tsx` polls `chat-session?id=` (composer disabled + "thinking on the box…"; a box error surfaces a Retry that re-resumes). **Finalize/verify** resume the session to emit the spec markdown / `## Verification` section; the worker (deterministic Node code holding the GH token + DB admin) commits to `main` + queues the `build` job — the box generates, the worker commits. New columns `box_session_id`/`turn_status`/`last_error` (migration `20260620120000_roadmap_chats_box_session.sql`); new `.claude/skills/spec-chat/` skill. See [[../specs/box-spec-chat]].

**Shipped (2026-06-19, repo skills catalog):** the build/ops procedures the box worker + interactive sessions draw on are now **committed Claude Code skills** in `.claude/skills/` (P0–P3 — `build-spec`, `probe-db`, `write-migration`, `customer-remedy`, `fold-to-brain`, `backfill`, … 19 in all), each cross-linking its source recipe/script genre, atop a shared `scripts/_bootstrap.ts` foundation + `script-conventions` skill. The catalog + the runtime-action-vs-Claude-skill distinction live in [[../recipes/README]] § Build & ops skills. The batch fold-build still runs self-contained canonical instructions in `runFoldJob`; the committed `fold-to-brain` skill is the same procedure as a reusable draft.

**Known gaps / future:** worker concurrency vs Max rate limits (start 1–2); "commit without deploy" for status-only edits (deferred); the **New spec from brain** picker is a typed brain-slug (no autocomplete over ~600 pages yet); a **post-merge `brain:index` reconcile** on `main` (re-run the generator after merges so counts never drift) is not yet automated — runnable via `npm run brain:index`; a **Report-Issue scope guard** (pause a vague Report-Issue on `needs_input` asking for specifics instead of running a no-op fix-build) was scoped but never shipped — open a fresh narrow spec if wanted (was the unshipped Phase 3 of the superseded `build-no-op-visibility`; the shipped no-op → `needs_attention` + enqueue-confirmation halves landed via [[../specs/fix-report-issue-dropped]] / #155).

## Related

[[../specs/roadmap-build-console]] · [[../specs/build-approval-gates]] · [[../recipes/README]] · [[../specs/fold-build-batching]] · [[../integrations/slack-roadmap-console]] · [[../libraries/roadmap-actions]] · [[../libraries/slack-home]] · [[../inngest/slack-roadmap-notify]] · [[../inngest/brain-index-refresh]] · [[../libraries/brain-index]] · [[../dashboard/roadmap]] · [[../dashboard/branches]] · [[../archive]] · [[../tables/agent_jobs]] · [[../tables/worker_heartbeats]] · [[../tables/pending_folds]] · [[../tables/roadmap_chats]] · [[../libraries/roadmap-chats]] · [[../recipes/build-box-setup]] · [[../recipes/manage-the-build-queue]] · [[../project-management]] · [[agent-todo-system]]
