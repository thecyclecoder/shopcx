# Harden the Ticket Improve agent — roll persistent coaching into its mandate ✅

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-06-25:** the **Ticket Improve** agent (`ticket-improve`) sits at **5.8/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When Tilly consistently finishes ticket-improve jobs with a 'completed' status but leaves no visible before/after diff, no summary of categorization or tag changes, and no confirmation that customer voice was preserved — making it impossible for graders to verify rubric compliance.:** When you finish processing a ticket, before closing the job emit a structured summary that includes: (1) the original text snippet vs. the revised text snippet, (2) any category or tag changes made (old → new), and (3) an explicit statement that customer voice and meaning were preserved (or flagging where they were not) — write this to the job log or output field so it is visible in the log tail. — Graders can only score what they can see; every low grade notes the same root problem — no evidence of the actual changes made. Surfacing a concise before/after diff and change rationale in the log tail directly satisfies the rubric's verifiability requirement and removes the information gap that is capping every job at 5-6/10 regardless of whether the underlying work was correct.
- **When Every low-graded run completed without errors but left no visible record of what changed — no before/after diff, no summary of tag/category decisions, no confirmation of customer-voice preservation — making rubric compliance unverifiable.:** When you finish modifying a ticket, emit a structured change summary before closing the job: list (1) each field changed with old→new values, (2) tags added/removed, (3) categorization decision with rationale, and (4) a one-line confirmation that the customer's original wording was preserved or quote the key phrase kept intact. — Graders can only score what they can observe; a clean 'completed' status with no diff is indistinguishable from a no-op and caps scores around 6 regardless of actual work quality. An explicit audit trail directly proves the rubric criteria — genuine improvement, correct categorization, preserved customer voice — and also forces the worker to self-check those criteria before closing, catching drift before it is graded.

## Phase 1 — bake the coaching into the agent ✅
- ✅ shipped
- Baked the change-summary mandate into the **permanent prompt** of the `ticket-improve` agent (`scripts/builder-worker.ts` → `runTicketImproveJob`): both the initial-turn and resume-turn prompts now require a structured `change_summary` envelope on every final JSON (text_diff before/after, field_changes old→new, tags ±, categorization rationale, customer-voice verdict + a quoted-key-phrase evidence line, plus a `no_changes:true` escape hatch for investigation-only turns).
- Baked the same mandate into the **skill** (`.claude/skills/ticket-improve/SKILL.md`) as a new "`change_summary` — MANDATORY on every turn" section under the output protocol, so the agent follows it by default outside the worker too.
- Wired the worker to **render the envelope as plain text at the head of `log_tail`** (`formatImproveChangeSummary` in `scripts/builder-worker.ts`) so graders + the profile view see the diff without parsing JSON. A missing envelope renders as `Change summary: (missing — agent did not emit the mandated change_summary envelope; flag for grader)` — a graceful failure mode, not a silent one. Replaces the ephemeral `agent_instructions` coaching path (which `runTicketImproveJob` did not consume anyway); the open coaching rows were already archived `recheck_status='rolled_in'` by the director's roll.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Ticket Improve). The director authored this from Ticket Improve's coaching ledger; building it hardens the agent so the coaching never has to repeat.

## Verification
- Open a Ticket Improve session against a real ticket and send any message (the Improve UI on a ticket page) → expect the `kind='ticket-improve'` `agent_jobs` row to complete `status='completed'`, and its `log_tail` to BEGIN with a `Change summary:` block (the before/after snippets, any `field_changes` old→new, tags ± lines, categorization rationale, and a `customer voice: preserved — …` line carrying a quoted key phrase). The block appears whether the turn ended in `reply` or `propose`.
- Force a regression: temporarily edit the agent's final JSON to omit `change_summary` → expect the worker's log_tail to render `Change summary: (missing — agent did not emit the mandated change_summary envelope; flag for grader)`, NOT a silently empty tail. (The grader can then mark the turn down on rubric-visibility.)
- Send a pure-investigation message ("when does this customer's next box ship?") → expect the agent's `change_summary` to carry `no_changes: true` with a one-line note, and the rendered block to read `- no_changes: true — <note>` (no spurious before/after lines).
- Confirm both the worker prompt and the skill agree: grep `scripts/builder-worker.ts` for `MANDATE — every final JSON MUST carry` (present in both initial + resume branches) and `.claude/skills/ticket-improve/SKILL.md` for `change_summary` — MANDATORY on every turn (present once under Output protocol). A divergence between the two = the bake-in missed one surface.
- Over the next graded window: expect the Ticket Improve agent's grade rollup to climb back above the COACH_LOW_ROLLUP bar (≥ 7/10) on `agent_action_grades`. If a `low-graded run with no visible before/after` class recurs (a low grade citing the same rubric-visibility gap), the bake-in missed it — re-coach + re-roll.
