# Drop transient Inngest http_unreachable transport noise from the Control Tower inngest feed ⏳

**Owner:** [[../functions/platform]] · **Parent:** extends [[../specs/control-tower]] + [[../specs/error-feed-monitoring]] (sibling of [[../specs/error-feed-drop-bare-502-proxy-wrapper]] + [[../specs/error-feed-drop-aborted-stream-noise]]) · **Verdict:** monitor-false-positive
**Repair-root-cause:** `src/lib/inngest/inngest-failure-capture.ts (add a reusable istransientinngesttransporterror(errname, errmessage) classifier in src/lib/control-tower/error-feed.ts matching http_unreachable / reset the connection / unexpected ending response / performing request to sdk url; on a first-sight match auto-resolve as transient instead of opening+paging, escalating only on recurrence within a window; add a regression fixture using this 06e8cf82e141fbaa blob in error-feed.test.ts; gate on npx tsc --noemit)::monitor-false-positive`
**Repair-signature:** `inngest:06e8cf82e141fbaa`

Close the gap in the inngest-failure-capture path so a non-actionable Inngest TRANSPORT failure (Inngest couldn't get a clean reply from our Vercel SDK URL — a deploy-boundary Lambda reap / momentary connection reset, not an application throw) does not mint a fresh open incident that pages Platform owners on a loop that already recovered on its next beat. Add a small, reusable transient-transport classifier (mirroring the existing isBareLifecycle / isAbortedStreamNoise helpers in error-feed.ts) and route a first-sight match to auto-resolve-as-transient, escalating to a real open/paging incident only if the same signature RECURS within a window — so a genuine chronic timeout (the function actually too heavy / always failing) still surfaces, while one-off deploy-boundary blips are dropped.

## Problem (from Control Tower signature `inngest:06e8cf82e141fbaa`)
Signature inngest:06e8cf82e141fbaa opened (count=1, single timestamp 2026-06-24T00:12:10Z) for `shopcx-platform-director-cron: {"error":{"error":"http_unreachable: Error performing request to SDK URL: Your server reset the connection while we were reading the reply: Unexpected ending response"...}}` on trigger inngest/scheduled.timer. `http_unreachable` is Inngest's transport-layer error class — the SDK URL (our Vercel deployment) reset the connection while Inngest was reading the reply; the cron's own body (src/lib/inngest/platform-director-cron.ts) never threw — every loop is wrapped in step.run and the next 15-min beat succeeded. src/lib/inngest/inngest-failure-capture.ts calls recordError() for every our-app inngest/function.failed with no filter for this transport class, and the outage-aware auto-resolve in src/lib/control-tower/error-feed.ts only fires inside a Claude-breaker window, so this single Vercel↔Inngest transport reset minted an open incident and paged owners. This is a recurring, non-actionable class (deploy reaps against a */15 cron) identical in genre to the bare-502 and aborted-stream noise already filtered.

**Likely target:** `src/lib/inngest/inngest-failure-capture.ts (add a reusable isTransientInngestTransportError(errName, errMessage) classifier in src/lib/control-tower/error-feed.ts matching http_unreachable / 'reset the connection' / 'Unexpected ending response' / 'performing request to SDK URL'; on a first-sight match auto-resolve as transient instead of opening+paging, escalating only on recurrence within a window; add a regression fixture using this 06e8cf82e141fbaa blob in error-feed.test.ts; gate on npx tsc --noEmit)`

## Phase 1 — close it ⏳
Scope from the problem above; land the fix + its brain page; gate on `npx tsc --noEmit`.

## Verification
- Re-trigger the originating condition (signature `inngest:06e8cf82e141fbaa`) → expect no new error_events row / loop_alert for it, and the Control Tower tile stays green.

> Authored by the box Repair Agent from Control Tower signature `inngest:06e8cf82e141fbaa` (verdict: monitor-false-positive). Commission the build from the Control Tower / Roadmap board.
