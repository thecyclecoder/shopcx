# Harden the Fenn agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-02:** the **Fenn** agent (`fold`) sits at **6.7/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When Fenn gates fold-eligibility on the stored public.specs.status column (per fold-to-brain/SKILL.md:12 and :33) instead of the derived phase rollup, so genuinely-shipped specs whose stored column still reads planned/in_progress are released as false no-ops and folded by a later job minutes on.:** At the START of every fold job, decide a spec is foldable from the DERIVED phase rollup, never the stored specs.status column: a spec is shipped iff every public.spec_phases row is shipped (getRoadmap → deriveSpecCardStatus → rollupPhaseStatus). If the stored column reads planned/in_progress but all phases are shipped, it is STALE — proceed to fold, produce the brain-page edits + archive entry, and let the worker flip the specs/pending_folds row to status='folded' via the specs-table SDK. Only STOP when the phase rollup itself is non-shipped. — builder-worker.ts:7235-7242 documents that the stored specs.status is vestigial and 'frequently still reads planned/in_progress on a genuinely-shipped spec,' while the canonical shipped-ness is the rollup at brain-roadmap.ts:436 (deriveSpecCardStatus→rollupPhaseStatus) — the SKILL.md:12/:33 precondition Fenn followed reads the stale column, which is exactly the false no-op the four completed low grades share (each spec's phases were all shipped seconds before job creation, yet released as non-shipped).
- **When when the implementation PR already placed all durable knowledge in the correct brain home and the fold diff only creates archive.d metadata:** verify the existing brain content and report the action explicitly as archive-only; do not claim pages were folded or cross-linked unless the diff actually changed them — PR #948 changes only docs/brain/archive.d/agents-sidebar-kpis-and-profile-redesign.md:1, although its conclusion claimed docs/brain/dashboard/agents.md was folded during that action.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `fold` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Fenn). The director authored this from Fenn's coaching ledger; building it hardens the agent so the coaching never has to repeat.
