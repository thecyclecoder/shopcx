# Harden the Fenn agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-02:** the **Fenn** agent (`fold`) sits at **6.5/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When when folding a bug-fix into a brain Gotcha/Known-fixes note and you're about to write that a guard applies to 'both'/'all'/'every' entry point or caller:** grep the actual callers of the guard function in the merged source and name only the ones that truly call it — write 'authorSpecRowStructured normalizes; the markdown path relies on parseSpec' rather than a blanket 'both entry points call normalizeOwnerSlug' — In PR #959 the author-spec.md gotcha claimed 'Both entry points call normalizeOwnerSlug', but origin/main:src/lib/author-spec.ts shows only line 459 (authorSpecRowStructured) calls it — authorSpecRowFromMarkdown writes owner: card.owner ?? "" unnormalized. A fold's whole value is accuracy; a scope overclaim points a future reader at the wrong function, and this is the recurring accuracy slip dragging the rolling average to 6.6.
- **When Fenn concludes a fold is finished or a no-op after touching only the pages it first opened (or trusting the stored specs.status), leaving the fold's full artifact set incomplete — a sibling brain page still describing the reverted concept and/or the owed archive.d/{slug}.md index line never written.:** Before you conclude ANY fold, enumerate the complete owed artifact set and only flip the specs row to status='folded' once all of it exists: (1) grep the ENTIRE docs/brain tree for the folded spec's old symbol/prior-spec-slug (e.g. `maybeEnqueuePreMergeSecurityOnAccumulation` / `consolidate-premerge-checks-one-session`) and update EVERY page still describing it in present tense — not just the two or three pages you opened first; (2) ALWAYS write the docs/brain/archive.d/{slug}.md index line, even when the brain content already looks present — 'brain content present' is not 'archived cleanly', and a missing archive line with shipped siblings is an inconsistent state; (3) confirm 'shipped' from the derived phase rollup (getRoadmap / deriveSpecCardStatus), never the stored specs.status column, before ever deciding a candidate is unfoldable or a no-op. — Grounded in the diffs: #955 (git show 319a1d62) isolated the security verdict in agent-jobs.md + security-agent.md but left docs/brain/libraries/spec-test-runs.md:82-89 still calling maybeEnqueuePreMergeSecurityOnAccumulation 'now inert' under the FUSED model; and job 9a553d32 concluded 'already folded' for error-feed-drop-safari while docs/brain/archive.d/ holds all 6 sibling error-feed-drop-*.md pages but not that slug — both are the same skipped completeness check the stale-status cluster (builder-worker.ts:7235-7257) also fails.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `fold` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Fenn). The director authored this from Fenn's coaching ledger; building it hardens the agent so the coaching never has to repeat.
