# Storefront Optimizer — activation + product-scope gate (OFF by default) ✅

**Owner:** [[../functions/growth]] · **Parent:** the control surface for [[../goals/storefront-optimizer]]; gates [[storefront-optimizer-agent|M4]] + [[storefront-experiment-bandit-framework|M1]]. · **Found in design 2026-06-22:** the optimizer specs say the agent "auto-runs within policy" + scope is "Amazing Coffee" — but **no policy object / on-switch / enforced scope exists**. As spec'd, once M4's cron ships it would **auto-run live experiments on customer traffic with no explicit owner enablement** — violating supervisable autonomy. The ad iteration engine got this right (`iteration_policies.policy_active` → "no active policy, zero autonomous actions"); the storefront side must mirror it.

## Model — per-campaign approval (the owner taps "Build" to run each test)
The control model the owner chose (2026-06-22): the agent **proposes** campaigns; **each proposed campaign is surfaced as a card with a one-tap "Build / Approve"**, and **tapping it is the owner's approval to run that test** (exactly like approving a spec build / a repair fix). Nothing runs on live traffic without that per-campaign tap — even reversible levers. This is *more* conservative than auto-run and is the starting mode; letting reversible levers auto-run (skip the tap) is a later Growth-director opt-in, not the default.

- **A `storefront_optimizer_policy` row** (per workspace): `active boolean`, `product_scope` (allowlist of `product_id`s — Amazing Coffee only to start), `auto_run_reversible boolean DEFAULT false` (the later opt-in above), plus editable guardrails (max concurrent experiments, min sample, holdout %, auto-rollback thresholds). Agent-legible + agent-writable so the **Growth director** operates it later — the engine **never writes its own policy**.
- **`active` = "the agent proposes campaigns at all."** When `active=true`: the loop runs, and each due `(product × lander × audience)` campaign is **proposed as a card** (hypothesis + cited funnel/lever reasoning + the variant it would test) in `needs_approval` — waiting for the owner's **Build/Approve** tap. On approval → the M1 experiment goes `running` (sticky variant + exposures). On the tap being withheld → nothing runs. When `active=false` → the agent doesn't even propose (fully idle).
- **Table default `active=false` (safe for any new workspace); SEED the Superfoods workspace `active=true`, `product_scope=[amazing-coffee]`, `auto_run_reversible=false`** — so the optimizer is **on in propose-and-approve mode, scoped to Amazing Coffee**, the moment M4 ships: it proposes, you tap to approve each test.
- **Scope is enforced, not narrative.** Every proposal + activation checks `product_id ∈ product_scope` — it *cannot* touch a non-scoped product even if a lander exists.
- **Control surface:** a dashboard on/off + per-product scope + the `auto_run_reversible` toggle (the Growth control surface). Offer/structural levers stay approval-gated regardless.

## Verification

### Phase 1 — testable now (table + seed + gate + control surface)
- Run `npx tsx scripts/apply-storefront-optimizer-policy-migration.ts` → expect `✓ public.storefront_optimizer_policy has N columns` and `✓ seeded 1 active policy — active=true, product_scope=["ea433e56-…"], auto_run_reversible=false`.
- `select active, product_scope, auto_run_reversible from storefront_optimizer_policy where workspace_id=<superfoods>` → `active=true`, `product_scope=[amazing-coffee]`, `auto_run_reversible=false` (on, in propose-and-approve mode, scoped). For any *other* workspace there is no row → treated as OFF.
- On `/dashboard/storefront/optimizer` (as owner/admin): the master toggle reads ON, Amazing Coffee is checked in Product scope, Auto-run reversible is OFF, and the guardrails show the seeded defaults. Toggle the master switch OFF then ON → expect each change persists (re-load the page → state preserved); the PATCH is owner/admin-gated (a non-admin GET still reads, but PATCH → 403).
- Check a scoped product OFF then a non-existent id via the API → `PATCH /api/workspaces/[id]/storefront-optimizer-policy {"product_scope":["<not-a-product>"]}` → expect `400 product_scope contains ids not in this workspace`.
- Gate unit behavior (`src/lib/storefront/optimizer-policy.ts` `evaluateProposalGate`): `null`/`active=false` → `disposition:"idle"`, `canPropose:false`; active + out-of-scope product → `disposition:"refused_scope"`, `canPropose:false`; active + in-scope + `offer`/`structural` → `needs_approval`; active + in-scope + `reversible` with `auto_run_reversible=false` → `needs_approval`, with `true` → `auto_run`.

### M4 / M1 behavior (verifiable once the optimizer agent ships)
- After build + seed: `select active, product_scope, auto_run_reversible from storefront_optimizer_policy where workspace_id=<superfoods>` → `active=true`, `product_scope=[amazing-coffee]`, `auto_run_reversible=false` (on, in propose-and-approve mode, scoped).
- With `active=true` + `auto_run_reversible=false` (the seed): when M4 proposes a campaign, it lands as a **`needs_approval` card with a Build/Approve tap**; until tapped, `select count(*) from storefront_experiments where status='running'`=0 and no `experiment_exposure` events fire. Tapping **Build/Approve** → the experiment goes `running` (sticky variant + exposures begin).
- Scope enforced: a proposed/activated experiment for a non-`product_scope` product is refused (not just unscheduled), even if a lander exists.
- `active=false` → the agent doesn't propose at all (idle). An offer/structural lever stays approval-gated regardless of `auto_run_reversible`.
- Negative: with no approval tap, there is no path by which a campaign mutates live storefront content or assigns a live variant.

## Phase 1 — the policy table + per-campaign-approval gate + product scope + seed + toggle ✅
- ✅ code shipped; migration applied. `20260627120000_storefront_optimizer_policy.sql` (+ seed) applied via `scripts/apply-storefront-optimizer-policy-migration.ts` — run **off-box by the owner** (the build box's raw-Postgres-pooler `:6543` channel produced no output and timed out across 5 box attempts; the apply script is now self-diagnosing, printing one compact `>>> APPLY RESULT: …` line on every exit path so the next run surfaces the exact pg error or the failing statement). Gate library `src/lib/storefront/optimizer-policy.ts` (`loadOptimizerPolicy` / `evaluateProposalGate`). Control surface `src/app/dashboard/storefront/optimizer/page.tsx` + `src/app/api/workspaces/[id]/storefront-optimizer-policy/route.ts` (GET/PATCH, owner/admin write). Brain: `tables/storefront_optimizer_policy.md`, `libraries/storefront-optimizer-policy.md`, `dashboard/storefront__optimizer.md`. Verify with the `## Verification` checklist (the seeded `select`, the dashboard toggle, the `400`/`403` gates, and the `evaluateProposalGate` unit cases).
- `storefront_optimizer_policy` (table default `active=false`; **SEED the Superfoods workspace `active=true`, `product_scope=[amazing-coffee]`, `auto_run_reversible=false`** in the migration/apply script); M4 proposes each campaign as a `needs_approval` card (the Build/Approve tap runs it) reading this policy + enforcing scope via `evaluateProposalGate`; M1 only assigns a live variant for an approved (`running`) experiment (already enforced by [[storefront-experiment-bandit-framework|storefront-experiments]] `loadActiveExperiments`' `running`/`promoted` status filter); dashboard on/off + scope + `auto_run_reversible` toggle. Brain: [[../goals/storefront-optimizer]] · [[storefront-optimizer-agent]] · [[storefront-experiment-bandit-framework]] · [[../operational-rules]] (§ North star) · mirrors [[storefront-iteration-engine]] `iteration_policies`.
