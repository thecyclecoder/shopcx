# The Storefront Optimizer agent (campaign loop + build-or-request) ✅

**Owner:** [[../functions/growth]] · **Parent:** M4 — The Storefront Optimizer agent
**Blocked-by:** [[storefront-experiment-bandit-framework]], [[storefront-lever-importance-memory]], [[storefront-ltv-proxy-reconciler]], [[storefront-optimizer-activation-gate]]

The capstone of the [[../goals/storefront-optimizer]] — the standing **employee** agent that ties the foundations together. Its boss is the [[../functions/growth|Growth director]], which sets its objective + guardrails and grades each campaign ([[storefront-campaign-grading-loop|M5]]); the chain is CEO → Growth → Optimizer ([[../operational-rules]] § North star). It is a new box [[../tables/agent_jobs|agent_jobs]] **`kind`** that runs the **campaign loop** (one campaign = one hypothesis, one atomic lever): read the funnel + the [[storefront-lever-importance-memory|lever-importance map]] → form a grounded hypothesis → produce the variant (a content/config patch, or a hero via the Nano-Banana skill [[../libraries/gemini]]) → stand up an [[storefront-experiment-bandit-framework|M1 Thompson-sampling bandit]] vs holdout → run to significance on the [[storefront-ltv-proxy-reconciler|M3 predicted-LTV proxy]] → promote winner / kill loser (auto-rollback handled by M1) → commit the learning to M2 memory → receive a grade. It is **autonomous within policy** (reversible copy/hero/chapter changes on DB-driven landers) and **approval-gated** for offers + structural rewrites. When a hypothesis needs a capability that doesn't exist, it **authors a spec → routes it to the build box** (the [[repair-agent]] build-or-request pattern), the component ships, and the new lever enters its toolbox — the optimizer *extends* the storefront over time. Scope: **Amazing Coffee × all four lander types** (PDP, listicle, before/after, advertorial). Serves predicted-LTV-per-visitor + a rising average campaign grade.

## Phase 1 — the `storefront-optimizer` agent_jobs kind + lane ✅
- ✅ shipped
- Added a `kind='storefront-optimizer'` to [[../tables/agent_jobs]] (free-text kind, no migration — like `migration-fix`/`repair`); claimed on its own concurrency-1 lane (`MAX_STOREFRONT_OPTIMIZER=1`) in `scripts/builder-worker.ts` (`runStorefrontOptimizerJob`, a top-level `claude -p` on Max with read-only DB, no `ANTHROPIC_API_KEY`). Registered as a Control Tower tile (`agent:storefront-optimizer`, owner growth).
- Trigger: [[../inngest/storefront-optimizer]] `storefrontOptimizerCron` (daily `30 14`) → `enqueueDueCampaigns` enqueues one campaign cycle per DUE `(product × lander-type × audience)` (gated by `nextLeverToTest` ≥ `MIN_LEVER_SCORE_TO_TEST` + no active experiment + no live job on that surface). Mirrors [[box-escalation-triage]] / [[repair-agent]] enqueue-dedup discipline (≤1 active campaign per surface).

## Phase 2 — read state → form a grounded hypothesis ✅
- ✅ shipped
- `loadOptimizerBrief` ([[../libraries/storefront-optimizer-agent]]) reads: the funnel signal (`computeChapterPriorsFromFunnel` over [[../tables/storefront_events]] chapter dwell / CTA share), the [[storefront-lever-importance-memory|lever-importance map]] (`nextLeverToTest` + ranked candidates), the current predicted-LTV-per-visitor ([[../tables/storefront_ltv_metrics]] latest snapshot) + calibration state, the activation gate ([[storefront-optimizer-activation-gate]]), and the live lander content ([[../tables/advertorial_pages]]). It bakes the read-only state into the box session's prompt.
- The box session emits ONE atomic hypothesis: a single lever on a single `(product × lander-type × audience)`, reasoned from the **CRO principles** (the prompt enforces benefit/pain over features, hero dominant, pricing clarity #2, message-match, one clear CTA) — and **surfaces its reasoning** (the proposal carries `reasoning` citing the funnel signal + the lever's posterior). The worker rejects a proposal whose `lever_key` isn't a ranked candidate.

## Phase 3 — produce the variant + stand up the campaign ✅
- ✅ shipped
- The session proposes a reversible variant: a content patch (`VariantPatch` — copy/chapter add-remove-reorder) **or** `kind:'hero'` + a hero prompt; for a hero the WORKER generates it via `generateCampaignHero` → `generateNanoBananaProCombine` ([[../libraries/gemini]]) and uploads to the `product-media` path convention (mirrors [[../lifecycles/advertorial-landers]] `ensureReasonsHero`), then sets `patch.heroImageUrl`. The box session never calls the image API.
- `materializeCampaign` creates the `storefront_experiments` row (status `running`, carrying the `hypothesis`) + a control arm vs the variant arm (M1) vs holdout. ≤1 active campaign per surface; conservative mode reserves a bigger holdout. The bandit + attribution + auto-rollback are M1's — the agent only authors the hypothesis/variant. Auto-runs when the policy opts reversible levers into `auto_run_reversible`; otherwise surfaced as a `storefront_campaign` Approve card.

## Phase 4 — decide → learn → report ✅
- ✅ shipped (already wired by M1)
- On the M1 bandit reaching significance (or auto-rollback firing), [[storefront-experiment-refresh]] promotes the winner / confirms the kill, then **commits the learning to [[storefront-lever-importance-memory|M2 memory]]** via `updatePosterior` (win *or* loss) on every terminal decision — already wired in `commitLearning`. The `storefront_experiments` row IS the campaign record (hypothesis, lever, variant patch, `last_decision`, proxy rollups) that [[storefront-campaign-grading-loop|M5]] grades off `experiment_id`.
- Report up: the experiment + its run record ([[../tables/storefront_experiment_runs]]) surface on the [[../dashboard/storefront__funnel|funnel dashboard]]; the full Growth-director report contract is M5's (blocked by this spec).

## Phase 5 — missing-tool → build-or-request ✅
- ✅ shipped
- When a hypothesis needs a capability that doesn't exist (a video hero, a comparison-table widget, a new chapter type, an offer/structural change), the session emits `needs_build` and the worker (`authorOptimizerSpec`) does NOT fake it: it **authors a single-phase feature spec** (`**Owner:** [[../functions/growth]]` + a parent, scoped) committed to main and **surfaces it for owner Build** (the [[repair-agent]] surface-don't-auto-build pattern — `needs_approval` + a `storefront_build` `pending_actions` entry carrying the slug). On Approve the worker queues a `build` job (deduped via `hasActiveBuildForSlug`); once the component ships it registers in [[storefront-lever-importance-memory|M2]]'s `storefront_levers` and enters the agent's toolbox.
- Default is **surface for one-tap Build**, never silent auto-build (the [[repair-agent]] north-star rationale: authoring code/PRs is higher-stakes autonomy, owner-gated).

## Safety / invariants
- **Autonomous only within policy.** Reversible copy/hero/chapter changes on DB-driven landers auto-run; **offer changes + structural rewrites are approval-gated** (offers are [[storefront-dynamic-renewal-offers|M6]], owner-approved — they bleed margin on every renewal). Hitting a guardrail **escalates**, never executes ([[../operational-rules]] § North star).
- **One atomic lever per campaign** — clean attribution; never bundle two changes into one experiment.
- **Surfaces its reasoning.** Every hypothesis cites the funnel signal + lever posterior it came from; a silent optimizer is invisible to its supervisor.
- **Conservative until calibrated.** Honors M3's `isProxyCalibrated` — smaller bets + tighter thresholds until the reconciler has calibrated once.
- **Build-or-request, never fake.** A missing capability becomes a surfaced spec for owner Build; the agent never edits product code directly or auto-builds (mirror [[repair-agent]]).
- **Hard CRO rails:** no disease claims, no fabricated stats, brand voice (supplement compliance is non-negotiable — the goal § hard rails).
- **Deduped + bounded queue:** ≤1 active campaign per `(product × lander-type × audience)`; an undiagnosable / stuck cycle surfaces, it doesn't loop.

## Completion criteria
- A `storefront-optimizer` agent_jobs kind runs on its own lane, enqueued per due `(product × lander-type × audience)`, deduped to ≤1 active campaign per surface.
- The agent reads funnel + lever map + proxy, emits ONE atomic, reasoning-cited hypothesis from the CRO principles, and stands up an M1 campaign (content patch or Nano-Banana hero) vs holdout.
- On significance/rollback it promotes/kills, commits the learning to M2 (win or loss), and reports a graded campaign record up to Growth.
- A hypothesis needing a missing capability authors a scoped spec and surfaces it for owner Build (no fake, no silent auto-build); a shipped component registers as a new lever.
- Offer/structural changes are routed to approval, never auto-executed; the agent honors `isProxyCalibrated`.
- Demonstrated end-to-end on Amazing Coffee across all four lander types.

## Verification
- On the build box with the worker running, `insert into agent_jobs (workspace_id, kind, spec_slug, status, instructions) values ('<ws>', 'storefront-optimizer', '<product_id>:advertorial:all', 'queued', '{"workspace_id":"<ws>","product_id":"<product_id>","lander_type":"advertorial","audience":"all","lever_key":"<a component lever>"}')` (Amazing Coffee, policy `active=true` + in scope) → expect it claimed on the `storefront-optimizer` lane (worker log `claimed storefront-optimizer …`) and driven to a terminal/surfaced state, not stuck in `building`.
- `select status, kind, spec_slug, pending_actions from agent_jobs where kind='storefront-optimizer' order by created_at desc limit 1;` → with the default policy (`auto_run_reversible=false`) expect `status='needs_approval'` and a `pending_actions` entry `type='storefront_campaign'` carrying the typed plan (hypothesis citing a funnel signal + lever posterior in `log_tail`). On `/api/roadmap/approve` (→ `queued_resume`) expect the worker to stand up a `storefront_experiments` row (status `running`) with a control arm vs the variant arm: `select status, lever, hypothesis, holdout_pct from storefront_experiments where product_id='<product_id>' order by created_at desc limit 1;`.
- With a workspace whose policy has `auto_run_reversible=true`, enqueue a cycle → expect the experiment stood up immediately (job `completed`, `log_tail` starts `AUTO-RAN (reversible)`), no approval card.
- Enqueue a second cycle for the SAME surface while a campaign (running experiment) exists → expect the job `completed` with `log_tail` "deduped" and NO second experiment (≤1 active per surface).
- Let the M1 daily refresh drive that experiment to significance → expect the winner promoted and a `storefront_lever_importance` posterior updated for the lever (M2 `updatePosterior`, win or loss): `select lever_id, importance, n_tests, evidence from storefront_lever_importance order by updated_at desc limit 1;` — the experiment row is the campaign record M5 grades.
- Feed the box session a hypothesis requiring a non-existent capability (a video hero / comparison widget) so it returns `needs_build` → expect a scoped spec committed to `docs/brain/specs/` on main (owner growth) + `status='needs_approval'` with a `pending_actions` `type='storefront_build'` carrying the slug; it does NOT auto-build or fake the lever. On Approve expect a `kind='build'` job queued for that slug.
- Confirm conservative gating: with `getCalibrationState().calibrated=false`, a stood-up experiment's `holdout_pct ≥ 0.2` (`CONSERVATIVE_MIN_HOLDOUT`) and `last_decision.conservative=true`.
- Confirm the `agent:storefront-optimizer` tile is registered: it appears on the Control Tower dashboard (owner growth) and stays green when idle, red only past the stuck threshold.
