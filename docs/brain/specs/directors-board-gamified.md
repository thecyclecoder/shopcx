# Gamified #directors board (Messages) ✅

**Owner:** [[../functions/platform]] · **Parent:** M3 — Gamified #directors board
**Blocked-by:** [[agents-hub-role-inboxes]]

The **Messages** tab of the M1 inbox, built as a Slack-style **team channel** — not a log. Each director is a **character** (name, personality, color, SVG mascot from the [[agents-hub-role-inboxes]] persona module) posting conversationally: *"🛠️ Ada · Platform — squashed a 500 on the portal path, all green; escorting the Acquisition goal, 3/5 milestones down 💪"*. It's **two-way**: the CEO replies, `@`-mentions, or asks "why?" and the director answers — wired to the existing "answer why" brains (**dev-ask** [[../libraries/dev-message-threads]] for read-only investigation, **spec-chat** [[../libraries/roadmap-chats]] for spec context). Each director carries an **XP card** (specs shipped · bugs fixed · goals escorted · streak), and the day closes with an **EOD recap** standup post (*"Shipped 8 specs · advanced 1 goal · fixed 2 bugs · approved 4 migrations"*), extending the existing daily-report pattern ([[../libraries/daily-analysis-report]] `generateDailyReport`, today ticket-only) to a director standup. Today none of this exists — [[../tables/dashboard_notifications]] is a generic bell, with no personas, no board, no XP. Success metric served: the CEO **reads the board + the daily recap, not the details** — the human-legible top layer that makes the offload trustworthy.

## Phase 1 — the board store + conversational posts ✅
- ✅ shipped
- `director_messages` (columns: `id`, `workspace_id`, `author_function` / `author='ceo'`, `body`, `kind` ∈ `update｜reply｜recap｜approval-note`, `parent_message_id` (threading), `mentions text[]`, `metadata jsonb`, `created_at`) backing the Messages tab. Brain page [[../tables/director_messages]] (probe live schema first per [[../README]]).
- Render the channel in the M1 Messages tab: each post shows the author's persona chip + mascot ([[agents-hub-role-inboxes]] `agent-personas`), conversational human-readable body, timestamps, threads. The Platform director (M4) is the first real author; until then a seeded/system post proves the surface.

## Phase 2 — two-way reply wired to dev-ask / spec-chat ✅
- ✅ shipped
- The CEO replies / `@`-mentions a director / asks "why?" in-thread → route to the existing answer brains: **dev-ask** ([[../libraries/dev-message-threads]] `createThread`/`markThreadThinking` → box `dev-ask` [[../tables/agent_jobs]] kind) for read-only investigation, **spec-chat** ([[../libraries/roadmap-chats]] `saveChat`/`markTurnThinking`) when the post is about a specific spec (its `metadata.spec_slug`). The director's answer posts back into the thread as a `reply` message — no new LLM plumbing, reuse the box sessions. Routing + thread creation live in `director-board.ts` `routeBoardReply`; the CEO question (POST `/api/developer/agents/board`) seeds the brain thread with the board context and stamps a `BoardReplyLink` into the job's instructions so the worker (`postBoardAnswer` in `scripts/builder-worker.ts`) posts the box's answer straight back as a `director_messages` `reply`.
- `turn_status` (thinking/idle) surfaced inline so a pending "why?" shows the director is investigating, mirroring the dev-message-center thinking state — `enrichAwaiting` reads the linked thread's `turn_status` and flags the CEO reply `awaiting`; the channel polls every 4s while any reply is awaiting until the director's answer lands.

## Phase 3 — per-director XP card ✅
- ✅ shipped
- A derived XP view per function: **specs shipped** (merged builds owned by the function), **bugs fixed** (repair-agent / error-fix approvals it handled), **goals escorted** (milestones advanced — M4's job), **streak** (consecutive active days). Compute from existing signals — [[../tables/agent_jobs]] (merged builds, kinds), the M2 [[../tables/approval_decisions]] log, and [[../libraries/brain-roadmap]] goal/milestone completion — no new event capture where a count is already derivable.
- Render as a card on the director's row in the M1 Agents hub + atop its board channel.
- Shipped: [[../libraries/director-xp]] `getDirectorXp(workspaceId)` derives the four counts in one pass — `specsShipped` from merged `build` [[../tables/agent_jobs]] mapped to the live spec→owner map ([[../libraries/brain-roadmap]] `getRoadmap().specs[].owner`); `bugsFixed` from `approved` [[../tables/approval_decisions]] whose raising job is `kind ∈ {repair, regression}` by `raised_by_function`; `goalsEscorted` from shipped milestones across the function's owned goals; `streak` from consecutive active UTC days in [[../tables/director_activity]]. Surfaced by owner-gated `GET /api/developer/agents/xp` and rendered by `<XpCard>` (`src/components/agents/xp-card.tsx`) on each director's row in the Agents hub (atop its board channel) + on the director profile page. **Derived + display-only** — a gamified proxy, never an objective ([[../operational-rules]] § North star).

## Phase 4 — the EOD recap standup post ✅
- ✅ shipped
- A daily job (cron, mirroring [[../inngest/daily-analysis-report-cron]]) that, per active director, composes a standup recap from the day's activity (`approval_decisions`, merged builds, milestones advanced) and posts a `recap` message to the board **and** the M1 **Daily Summaries** tab. Extends [[../libraries/daily-analysis-report]] `generateDailyReport`'s aggregate-then-narrate shape to the director domain.
- The CEO recap is a roll-up across all directors (the company standup).
- Shipped: [[../libraries/director-recap]] `generateDirectorRecap(workspaceId, date)` aggregates per director — `specsShipped` (merged `build` [[../tables/agent_jobs]] in-day, owned via the [[../libraries/brain-roadmap]] spec→owner map), `bugsFixed` / `migrationsApproved` / `approvalsHandled` (in-day `approved` [[../tables/approval_decisions]] sliced by raising job kind), `goalsAdvanced` + `actions` ([[../tables/director_activity]] in-day) — then narrates **deterministically in each persona's voice** (no LLM, no API key; counts are a display-only proxy, [[../operational-rules]] § North star). Posts a `recap` [[../tables/director_messages]] message + an `agent_daily_summary` [[../tables/dashboard_notifications]] row (the **Daily Summaries** tab) per **active** director, then a **CEO roll-up**. **Idempotent** per `(workspace, date, author)` (`metadata.recap_date`) so the cron retry never double-posts; a quiet workspace returns `no_activity` (no empty standup). Driven by [[../inngest/director-recap-cron]] (`0 23 * * *`, registered in [[../libraries/control-tower]] `MONITORED_LOOPS`, owner `platform`).

## Safety / invariants
- **Personas are reskinnable config.** Names/mascots/colors come from the M1 `agent-personas` module — never hardcoded per component ([[../operational-rules]]).
- **Human-readable, not a log.** Posts are conversational prose (the goal § board), respecting the AI-voice rules where customer-facing tone matters; this is an internal owner channel, but stays plain and scannable.
- **Reuse the answer brains.** Two-way replies route to the existing dev-ask / spec-chat box sessions — do not stand up a parallel LLM chat path.
- **XP is derived, not gospel.** XP counts read from existing truth (jobs, decisions, goal completion); they are a **gamified proxy**, never an objective the directors optimize ([[../operational-rules]] § North star) — display-only.
- **Owner-only.** The board is the owner's channel for now (M1 owner-only sidebar); no member write access.

## Completion criteria
- A `director_messages` store backs the Messages tab; directors post conversational, persona-styled, human-readable updates with threading + mentions, with a brain page.
- Two-way reply / `@`-mention / "why?" routes to dev-ask + spec-chat and posts the answer back in-thread, reusing the existing box sessions.
- Each director shows an XP card (specs shipped · bugs fixed · goals escorted · streak) derived from existing signals.
- An EOD recap standup posts daily per director (+ a CEO roll-up) to the board and the Daily Summaries tab, extending the daily-report pattern.
- Brain pages written + cross-linked from [[../goals/devops-director]].

## Verification
- **(Phase 1, shipped)** On `/dashboard/agents` as the workspace owner, select any role → **Messages** tab → expect the Slack-style **#directors board** (the same workspace-wide channel for every role): each post shows a persona avatar + name/role, a conversational body with `@mentions` highlighted, a timestamp, and threaded replies. After running `npx tsx scripts/seed-director-board.ts`, expect three seeded posts — a **System** welcome, an **Ada · Platform** update ("🛠️ Squashed a 500…"), and a **CEO** reply threaded under Ada's post. `GET /api/developer/agents/board` returns `{posts:[{…,replies:[…]}]}`; the same endpoint returns **403** for a non-owner.
- On `/dashboard/agents` Messages tab, expect a Slack-style channel: persona chip + mascot per post, conversational body, timestamps, threaded replies — populated once the M4 director (or a seed) posts.
- **(Phase 2, shipped)** On `/dashboard/agents` Messages tab as the owner, click **Reply** under Ada's post, type "why did you approve that migration?" and Send → expect `POST /api/developer/agents/board` to 201 a CEO `reply`, a `kind='dev-ask'` row in [[../tables/agent_jobs]] (the linked `dev_message_threads` row flips `turn_status='thinking'`), and an inline **"Ada is investigating…"** indicator under your reply (the channel polls every 4s). Once the box turn finishes, expect Ada's answer to appear as a threaded `reply` (a `director_messages` row `author='director'`, `kind='reply'`, `metadata.source='dev-ask'`) and the investigating indicator to clear. A reply under a post whose `metadata.spec_slug` is set routes to `kind='spec-chat'` instead. `POST /api/developer/agents/board` returns **403** for a non-owner and **400** without `parentMessageId`+`body`.
- **(Phase 3, shipped)** On `/dashboard/agents` as the owner, open the **Inbox** view and select a director (e.g. **Ada · Platform**) → expect an **XP card** atop its board channel with four tiles: **🚢 Specs shipped · 🐛 Bugs fixed · 🎯 Goals escorted · 🔥 Streak**. The same card renders on the director's profile page `/dashboard/agents/{slug}` under an **XP** heading. The counts reconcile against the source tables: **specs-shipped** = `agent_jobs` rows `kind='build'` + `status='merged'` whose `spec_slug` is owned by `{slug}` (the spec's `**Owner:**` line); **bugs-fixed** = `approval_decisions` `decision='approved'` whose `agent_job_id` is a `kind ∈ {repair, regression}` job, by `raised_by_function='{slug}'`; **goals-escorted** = shipped milestones across the goals `{slug}` owns; **streak** = consecutive active UTC days for `{slug}` in `director_activity`. A director with no activity shows all-zero tiles + a "No XP yet" hint. `GET /api/developer/agents/xp` returns `{ xp: { [slug]: {specsShipped,bugsFixed,goalsEscorted,streak} } }`; the same endpoint returns **403** for a non-owner.
- **(Phase 4, shipped)** Trigger `director-recap-cron` (Inngest dashboard → `director-recap-cron` → Invoke, or wait for `0 23 * * *`). For a workspace that had director activity today (a [[../tables/director_activity]] row, an `approved` [[../tables/approval_decisions]] decision, or a merged `build` [[../tables/agent_jobs]]) → expect, per **active** director, a `kind='recap'` [[../tables/director_messages]] post (`author='director'`, `author_function={slug}`, `metadata.recap_date`=today) reading `{emoji} EOD recap — shipped N specs · advanced M goals · fixed K bugs · approved J migrations.` on the **Messages** board, **plus** an `agent_daily_summary` [[../tables/dashboard_notifications]] row titled `{name} · {role} — daily recap` in the owner's **Daily Summaries** tab on `/dashboard/agents`. Expect one **CEO roll-up** `recap` (`author='ceo'`, `metadata.scope='ceo-rollup'`) summing every active director (`👑 Company standup — … across D active directors.`) + a `Company standup — {date}` Daily Summary. The counts reconcile against the source tables for today's UTC window. Re-invoking the cron the same day posts **nothing new** (idempotent on `metadata.recap_date`). A workspace with no activity today gets **no** recap (the generator returns `no_activity`).
- The recap narration is **deterministic** (no Anthropic key needed): the body is composed from the aggregated counts in the persona's voice, so it renders identically on the box and in prod.
