# Storefront /policies/[slug] page + every cached storefront route's generateMetadata — wrap in `'use cache' + cacheLife` so cacheComponents prerendering unblocks the build

**Owner:** [[../functions/platform]] · **Parent:** extends [[../specs/regression-agent]] · **Regression-of:** [[dashboard-layout-wrap-uncached-auth-in-suspense-for-cachecom]]
**Regression-signature:** `regression:dashboard-layout-wrap-uncached-auth-in-suspense-for-cachecom:f4297b4605d8`

Finish the cacheComponents migration sweep for the storefront. The dashboard/help/portal/workspace-select layers are done; the storefront's /policies/[slug] page and the generateMetadata of every existing `'use cache'` storefront route still do top-level uncached Supabase reads, which under cacheComponents (PR #607) raises 'Uncached data was accessed outside of <Suspense>' on /policies/[slug] and 'generateMetadata that depends on uncached external data when the rest of the route does not' on /store/[workspace]/[slug|blog|blog/[handle]|links]. Next bails after the first 2 errors per 29-worker prerender batch, so vercel inspect surfaces only one of these per attempt — but the same commit fails on either depending on which batch lands first (confirmed: shopcx-6ku5yvgur reports /policies/[slug]; shopcx-felzxkurj reports /store/[workspace]/blog/[handle]). Sweep both in one pass with the existing `'use cache' + cacheLife` pattern used by every storefront cached default export.

## What regressed
The 'fresh Vercel deploy after the fix lands as READY' verification bullet from [[dashboard-layout-wrap-uncached-auth-in-suspense-for-cachecom]] (and the entire cacheComponents-migration chain it extends: [[pdp-edge-served-variant-use-cache]], [[pdp-edge-served-variant-use-cache-unblock-build-strip-route-]], [[pdp-edge-served-variant-use-cache-strip-revalidate-runtime-k]], [[pdp-edge-served-variant-use-cache-strip-dynamicparams]], [[pdp-edge-served-variant-use-cache-strip-dynamicparams]]). Every post-cacheComponents production deploy is ● Error; last READY deploy remains shopcx-nanphlyy4 (commit 3c01c95, pre-migration), so prod still serves the pre-migration code.

## Offending change
Commit c620d402 / PR #607 'pdp-edge-served-variant-use-cache' set `cacheComponents: true` in next.config.ts. Under cacheComponents (node_modules/next/dist/docs/01-app/02-guides/migrating-to-cache-components.md), top-level uncached IO in any statically prerendered route — page body OR generateMetadata — blocks the prerender. The cascade of fixes in #614/#618/#619/#623 cleared dashboard + help + portal + workspace-select but missed (a) src/app/(storefront)/policies/[slug]/page.tsx (default `PolicyPage` lines 75-82 and `generateMetadata` lines 64-73 both call `createAdminClient()` + workspace + policies queries with no `'use cache'`), and (b) the `generateMetadata` of the four already-cached storefront routes (/store/[workspace]/[slug] line 144, /store/[workspace]/blog line 41, /store/[workspace]/blog/[handle] line 51, /store/[workspace]/links line 22) — each calls `getPageData` / `getBlogWorkspaceBySlug` / `getBlogPost` at the top level while the default export below is `'use cache'`, triggering the 'generateMetadata that depends on uncached external data when the rest of the route does not' error.

## Phase 1 — restore it
Single mechanical sweep — add the `'use cache' + cacheLife(...)` pair to every offending function. Specifically: (1) src/app/(storefront)/policies/[slug]/page.tsx — at the top of `PolicyPage` add `"use cache"; cacheLife({ stale: 3600, revalidate: 3600, expire: 3600 });` and the same at the top of `generateMetadata`; add `import { cacheLife } from 'next/cache'`. (2) src/app/(storefront)/store/[workspace]/[slug]/page.tsx — at the top of `generateMetadata` (line 144) add the same `'use cache' + cacheLife(...)` pair (cacheLife already imported). (3) src/app/(storefront)/store/[workspace]/blog/page.tsx — same pair at the top of `generateMetadata` (line 41). (4) src/app/(storefront)/store/[workspace]/blog/[handle]/page.tsx — same pair at the top of `generateMetadata` (line 51). (5) src/app/(storefront)/store/[workspace]/links/page.tsx — same pair at the top of `generateMetadata` (line 22, can reuse links's tighter `{ stale: 300, revalidate: 300, expire: 300 }`). No generateStaticParams change needed; no segment-config exports (verification bullet from prior chain — `dynamicParams` / `revalidate` / `runtime` / `dynamic` must remain absent). Gate on `npx tsc --noEmit`, push, confirm the next production deploy lands READY on `vercel inspect`.
Gate on `npx tsc --noEmit`.

## Verification
- A fresh Vercel deploy after the fix lands as READY (not ERROR) — `vercel inspect <latest>` no longer shows 'Route "...": Uncached data was accessed outside of <Suspense>' (nor 'generateMetadata that depends on … uncached external data', nor 'dynamicParams is not compatible with cacheComponents', nor `revalidate`, `runtime`, `dynamic`).
- Repo-wide `next build` static page generation reaches 374/374 with no 'Uncached data was accessed outside of <Suspense>' AND no 'generateMetadata that depends on … uncached external data' line on any /policies/* or /store/* route (run `vercel inspect --logs <latest>` and confirm zero of either line).
- Repo-wide grep `export const dynamicParams` in src/ still returns zero hits (prior chain's invariant holds).
- Repo-wide grep across src/app/(storefront)/**/page.tsx confirms every default export that does uncached DB IO has `'use cache'` AND every generateMetadata in a `'use cache'` route also has `'use cache'` — no remaining bare async server pages at static paths in (storefront).
- curl https://shop.superfoodscompany.com/policies/returns and /policies/subscriptions → 200 OK, the policy markdown renders, AND on the second curl `x-vercel-cache: HIT` with `cache-control` carrying `s-maxage` (the new `'use cache'` body is actually cached).
- curl the served-arm PDP `https://shop.superfoodscompany.com/{handle}` with two distinct sticky `sx_variant` cookies → a different hero `<img>` URL per arm AND, on the second curl of each variant URL after warm-up, `x-vercel-cache: HIT` with `cache-control` carrying `s-maxage` (not `private, no-cache, no-store`).
- Bare PDP with no `?variant=`, no `_sxv` and no running experiment → `x-vercel-cache: HIT` (cached SSG/`'use cache'` render, no per-request server compute).
- Re-run spec-test on [[dashboard-layout-wrap-uncached-auth-in-suspense-for-cachecom]], [[pdp-edge-served-variant-use-cache-strip-dynamicparams]], [[pdp-edge-served-variant-use-cache]], and [[pdp-edge-served-experiments]] → all four previously-failing verification checks pass again (the whole cacheComponents migration chain's original ✅ holds).
- Sign into /dashboard at https://shopcx.ai and confirm the auth/redirect flow still works (unauthenticated → /login; no workspace → /workspace/select; member → sidebar renders) — the storefront sweep must not touch dashboard behaviour.
- Re-run spec-test on [[dashboard-layout-wrap-uncached-auth-in-suspense-for-cachecom]] → expect its previously-failing verification check(s) pass again (the original ✅ holds).

> Authored by the box Regression Agent — a confirmed regression of [[dashboard-layout-wrap-uncached-auth-in-suspense-for-cachecom]] (signature `regression:dashboard-layout-wrap-uncached-auth-in-suspense-for-cachecom:f4297b4605d8`). The DevOps Director queues the build (auto-approve within its leash; pre-M4 the CEO queues it).
