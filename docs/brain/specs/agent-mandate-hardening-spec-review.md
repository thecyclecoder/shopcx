# Harden the Vale agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-01:** the **Vale** agent (`spec-review`) sits at **4.6/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When On the completed sweeps, Vale's pass verdicts carry a bare one-sentence reason (or the run reduces to a ✅/⚠ tally) with no per-criterion evidence, so the grader cannot tell a genuine checklist walk from a rubber-stamp and caps the grade at 6.:** At the start of every sweep, for EACH spec write a reason that names the spec and states the RESULT of all six checklist checks explicitly — even on a pass: 'Owner [[../functions/X]] resolves; Parent resolves to mandate/milestone Y; phases 1–N contiguous & each carries ### Verification; no customer_id table' — never a bare 'passes'. Mirror the orphan-parent run that scored highest; a bare pass reason is your entire audit trail, so make it enumerate what you checked. — builder-worker.ts:8880-8881 persists only reason/defects[] per decision while the log_tail at :8889 is just the worker's ✅/⚠ count, so the reason IS the grader's audit trail; the one 9-caliber review (grade id 89328a41) enumerated 'Owner [[../functions/growth]] resolves, passed phases/blocked-by/verification, caught a REAL orphan-parent' whereas the four 6/10 passes gave no per-check evidence — matching SKILL.md:108-124's contract but under-filling the reason the rubric grades on.
- **When In the lowest-graded sweeps Vale returned a decision whose slug did not exactly match the queued `.box/spec-{slug}.md` filename (the queue's only spec, `sync-spend-route-through-graph-retry`, has a short slug but a long sentence-title), so the worker discarded the verdict as skipped and the spec was left un-triaged.:** When you emit each decision, copy the slug VERBATIM from the `.box/spec-{slug}.md` path in your queue — never re-derive or abbreviate it from the spec's H1 title/summary — and emit exactly one decision per queued slug with verdict spelled literally `pass` or `needs_fix`; the worker silently drops any decision whose slug isn't in the materialized queue, which turns an otherwise-correct review into a 0-findings 'skipped' no-op. — scripts/builder-worker.ts:8807 discards any decision where `!queuedSet.has(d.slug)` (→ skipped, no verdict applied) and :8810-8811 drops any verdict outside pass/needs_fix; the grade-5 jobs (87cb86ba, ba52610e) logged `reviewed 1/1 · ✅0 ⚠0 · skipped 1` over the single queued slug `sync-spend-route-through-graph-retry`, and its director_activity shows every landed needs_fix used that exact slug — so the skips were slug/verdict-string mismatches, not missed defects.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `spec-review` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Vale). The director authored this from Vale's coaching ledger; building it hardens the agent so the coaching never has to repeat.
