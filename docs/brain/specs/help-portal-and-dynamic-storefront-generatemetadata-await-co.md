# Help + portal + dynamic-storefront `generateMetadata` (and customize/checkout/thank-you bodies) — await connection() so cacheComponents prerendering unblocks the build past /policies

**Owner:** [[../functions/platform]] · **Parent:** extends [[../specs/regression-agent]] · **Regression-of:** [[storefront-policies-page-and-cached-route-metadata-use-cache]]
**Regression-signature:** `regression:storefront-policies-page-and-cached-route-metadata-use-cache:15fa3bc68927`

Finish the cacheComponents prerender sweep. The /policies + /store/[workspace] generateMetadata fix from [[storefront-policies-page-and-cached-route-metadata-use-cache]] landed (commit 9ca7263), but the next prod deploy (shopcx-apt9u3aby, commit 95c3d57) still ● Errors at `Generating static pages 93/374` with `Route "/help/[slug]/[articleSlug]": Uncached data was accessed outside of <Suspense>`. help/layout.tsx already has `await connection()` from PR #623 — that only covers page BODIES; `generateMetadata` has its own prerender pass that bypasses the layout. The two help generateMetadata functions, the two portal generateMetadata functions, and the three dynamic storefront pages (customize/checkout/thank-you — per-cart/per-order, NOT cacheable, NOT under any layout connection()) all still do top-level uncached `createAdminClient()` IO. Sweep them all in one pass with `await connection()` so the build reaches 374/374 and prod lands READY.

## What regressed
The whole cacheComponents-migration chain's central ✅ — 'a READY production deploy on the post-cacheComponents commit'. Every prod deploy since commit c620d402 (PR #607) is ● Error; last READY remains shopcx-nanphlyy4 (commit 3c01c95, pre-migration). With prod stuck pre-migration, the regressed spec's downstream bullets ALL fail: /policies/returns returns `x-vercel-cache: MISS` and `cache-control: private, no-cache, no-store` (the new `'use cache'` body isn't deployed); bare PDP `/super-greens` returns `x-vercel-cache: MISS` (the cached PDP render isn't live); dependent specs ([[dashboard-layout-wrap-uncached-auth-in-suspense-for-cachecom]], [[pdp-edge-served-variant-use-cache-strip-dynamicparams]], [[pdp-edge-served-variant-use-cache]], [[pdp-edge-served-experiments]]) all spec-test ❌ on the same READY-deploy bullet.

## Offending change
PR #607 / commit c620d402 turned on `cacheComponents: true` in next.config.ts. Under cacheComponents, top-level uncached IO in any statically prerendered route — page body OR generateMetadata — blocks the prerender. The migration cascade (#613/#614/#618/#619/#623/#624) cleared dashboard + cached storefront routes + help/portal/workspace-select BODIES, but two gaps survived: (a) `generateMetadata` is prerendered separately from the page body — a layout's `await connection()` does NOT propagate to children's generateMetadata, so help/[slug]/[articleSlug] + help/[slug] + portal/[slug] (layout) + portal/[slug]/login still fail. (b) The dynamic (storefront) routes /customize, /checkout, /thank-you have no `'use cache'` AND no layout `connection()` above them — (storefront)/layout.tsx is a pure passthrough — so both their bodies AND their generateMetadata still trip the prerender. Next.js bails fast on the first error per 29-worker prerender batch, so /help/[slug]/[articleSlug] is the only one currently named in the logs, but every other identical offender will surface the moment that one is fixed.

## Phase 1 — restore it
Single mechanical sweep — add `await connection()` (imported from `next/server`) at the top of every offending function. Specifically: (1) src/app/help/[slug]/[articleSlug]/page.tsx — `await connection()` at the top of `generateMetadata` (line 8); body is already covered by help/layout.tsx. (2) src/app/help/[slug]/page.tsx — same at the top of `generateMetadata` (line 8). (3) src/app/portal/[slug]/layout.tsx — same at the top of `generateMetadata` (line 22); body already covered by portal/layout.tsx. (4) src/app/portal/[slug]/login/page.tsx — same at the top of `generateMetadata` (line 12). (5) src/app/(storefront)/customize/page.tsx — `await connection()` at the top of `generateMetadata` (line 33) AND at the top of the default `CustomizePage` body (per-cart, not cacheable). (6) src/app/(storefront)/checkout/page.tsx — same pair (line 23 + body). (7) src/app/(storefront)/thank-you/page.tsx — same pair (line 18 + body). Add `import { connection } from 'next/server'` to any file that doesn't already have it. No segment-config exports added (the chain's `dynamicParams`/`revalidate`/`runtime`/`dynamic` invariant must remain — grep returns zero hits). Gate on `npx tsc --noEmit`, push, confirm the next production deploy lands READY on `vercel inspect`.
Gate on `npx tsc --noEmit`.

## Verification
- A fresh Vercel deploy after the fix lands as READY (not ERROR) — `vercel inspect <latest>` no longer shows 'Route "...": Uncached data was accessed outside of <Suspense>' (nor 'generateMetadata that depends on … uncached external data', nor 'dynamicParams is not compatible with cacheComponents', nor `revalidate`, `runtime`, `dynamic`).
- Repo-wide `next build` static page generation reaches 374/374 with no 'Uncached data was accessed outside of <Suspense>' AND no 'generateMetadata that depends on … uncached external data' line on ANY route (not just /policies/* or /store/*) — run `vercel inspect --logs <latest>` and confirm zero of either line.
- Repo-wide grep `export const dynamicParams\|export const revalidate\|export const runtime\|export const dynamic` in src/app/ still returns zero hits (prior chain's invariant holds).
- Repo-wide grep across src/app/**/page.tsx + layout.tsx confirms every `generateMetadata` that does uncached DB IO either starts with `'use cache'` (cacheable routes) OR `await connection()` (dynamic routes) — no remaining bare async generateMetadata calling createAdminClient or fetch-without-cache.
- curl https://shop.superfoodscompany.com/policies/returns and /policies/subscriptions → 200 OK, the policy markdown renders, AND on the second curl `x-vercel-cache: HIT` with `cache-control` carrying `s-maxage` (the original spec's `'use cache'` body is finally live).
- Bare PDP with no `?variant=`, no `_sxv` and no running experiment → `x-vercel-cache: HIT` (cached SSG/`'use cache'` render, no per-request server compute).
- curl /help/superfoods (and any /help/{slug}/{article}) → 200 OK with `x-vercel-cache: MISS` and `cache-control: private, no-cache, no-store` (the help subtree stays dynamic — the connection() guard works without caching).
- curl /portal/superfoods/login → 200 OK with `cache-control: private, no-cache, no-store` (portal stays dynamic).
- Re-run spec-test on [[storefront-policies-page-and-cached-route-metadata-use-cache]], [[dashboard-layout-wrap-uncached-auth-in-suspense-for-cachecom]], [[pdp-edge-served-variant-use-cache-strip-dynamicparams]], [[pdp-edge-served-variant-use-cache]], and [[pdp-edge-served-experiments]] → all previously-failing verification checks pass again (the whole cacheComponents migration chain's original ✅ holds).
- Sign into /dashboard at https://shopcx.ai and confirm the auth/redirect flow still works (unauthenticated → /login; no workspace → /workspace/select; member → sidebar renders) — the metadata sweep must not touch dashboard behaviour.
- Re-run spec-test on [[storefront-policies-page-and-cached-route-metadata-use-cache]] → expect its previously-failing verification check(s) pass again (the original ✅ holds).

> Authored by the box Regression Agent — a confirmed regression of [[storefront-policies-page-and-cached-route-metadata-use-cache]] (signature `regression:storefront-policies-page-and-cached-route-metadata-use-cache:15fa3bc68927`). The DevOps Director queues the build (auto-approve within its leash; pre-M4 the CEO queues it).
