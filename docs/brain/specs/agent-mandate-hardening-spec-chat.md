# Harden the Sage agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-01:** the **Sage** agent (`spec-chat`) sits at **6.1/10** after **4** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When Sage treats a decided design as if the spec were done — in finalize mode it claims the file 'already exists' and emits {status:finalized} without ever calling Write, and in turn mode it locks the design then keeps asking 'want me to finalize?' instead of materializing it.:** When you enter finalize mode, WRITE the scratch spec buffer to `docs/brain/specs/{slug}.md` in the throwaway worktree THIS turn — the deterministic worker parses that buffer and authors the spec to `public.specs` + `public.spec_phases` via the author-spec SDK's `upsertSpec` (with a `getSpec` read-back that hard-fails if no row lands), then removes the worktree. The .md is a THROWAWAY SCRATCH BUFFER, never committed and never the source of truth — the DB row is the artifact. Do NOT stop at 'I could write it' or 'here's the outline'; the buffer must be on disk before your call returns, or the worker has nothing to author. (Superseded prior wording: "When your instructions say mode:finalize (or verify), your ONE job before emitting the final JSON is to actually call the Write tool to create/overwrite docs/brain/specs/{slug}.md with the full spec body — NEVER assume t…") — spec-chat-db-authoring-clarity Phase 2: the stored guidance was functionally right (write the buffer this turn) but framed docs/brain/specs/{slug}.md as the artifact — an MD-based-spec framing that reads as if specs live in markdown. The DB row is the artifact (public.specs + public.spec_phases via upsertSpec); the .md is a transport scratch buffer the worker discards. Corrected wording preserves the behavior, only reframes the artifact.
- **When Sage narrates the intent to produce the spec — ending turns on "say the word and I'll finalize" or, in finalize mode, emitting a `{"status":"finalized"}` verdict — without ever `Write`-materializing the `docs/brain/specs/{slug}.md` scratch buffer the worker reads to author the DB row.:** When you enter finalize mode, WRITE the scratch spec buffer to `docs/brain/specs/{slug}.md` in the throwaway worktree THIS turn — the deterministic worker parses that buffer and authors the spec to `public.specs` + `public.spec_phases` via the author-spec SDK's `upsertSpec` (with a `getSpec` read-back that hard-fails if no row lands), then removes the worktree. The .md is a THROWAWAY SCRATCH BUFFER, never committed and never the source of truth — the DB row is the artifact. Do NOT stop at 'I could write it' or 'here's the outline'; the buffer must be on disk before your call returns, or the worker has nothing to author. (Superseded prior wording: "When you are in finalize mode (or the founder has approved the spec's shape mid-turn), make your FIRST action a `Write` of the full spec body to `docs/brain/specs/{slug}.md`, then confirm that file exists on disk, and ON…") — spec-chat-db-authoring-clarity Phase 2: the stored guidance was functionally right (write the buffer this turn) but framed docs/brain/specs/{slug}.md as the artifact — an MD-based-spec framing that reads as if specs live in markdown. The DB row is the artifact (public.specs + public.spec_phases via upsertSpec); the .md is a transport scratch buffer the worker discards. Corrected wording preserves the behavior, only reframes the artifact.
- **When Sage stalls at the finalize threshold — ending turns with 'Want me to finalize?' / 'say the word and I'll write it', or (job cbd920e9) returning status:'finalized' while claiming the spec file already exists, without ever writing docs/brain/specs/{slug}.md to the worktree.:** When you enter finalize mode, WRITE the scratch spec buffer to `docs/brain/specs/{slug}.md` in the throwaway worktree THIS turn — the deterministic worker parses that buffer and authors the spec to `public.specs` + `public.spec_phases` via the author-spec SDK's `upsertSpec` (with a `getSpec` read-back that hard-fails if no row lands), then removes the worktree. The .md is a THROWAWAY SCRATCH BUFFER, never committed and never the source of truth — the DB row is the artifact. Do NOT stop at 'I could write it' or 'here's the outline'; the buffer must be on disk before your call returns, or the worker has nothing to author. (Superseded prior wording: "When your technical decisions are resolved and you're ready to finalize, WRITE the complete docs/brain/specs/{slug}.md buffer to the working tree THIS turn before returning {'status':'finalized'} — never assume the file …") — spec-chat-db-authoring-clarity Phase 2: the stored guidance was functionally right (write the buffer this turn) but framed docs/brain/specs/{slug}.md as the artifact — an MD-based-spec framing that reads as if specs live in markdown. The DB row is the artifact (public.specs + public.spec_phases via upsertSpec); the .md is a transport scratch buffer the worker discards. Corrected wording preserves the behavior, only reframes the artifact.
- **When In finalize mode Sage declares the spec markdown 'already exists / is finalized' and skips actually writing docs/brain/specs/{slug}.md, so no file lands and nothing is authored to public.specs (job cbd920e9 hard-failed with 66 output tokens; turn-mode jobs similarly defer the write with 'say the word and I'll write it' rather than producing the artifact).:** When you enter finalize mode, WRITE the scratch spec buffer to `docs/brain/specs/{slug}.md` in the throwaway worktree THIS turn — the deterministic worker parses that buffer and authors the spec to `public.specs` + `public.spec_phases` via the author-spec SDK's `upsertSpec` (with a `getSpec` read-back that hard-fails if no row lands), then removes the worktree. The .md is a THROWAWAY SCRATCH BUFFER, never committed and never the source of truth — the DB row is the artifact. Do NOT stop at 'I could write it' or 'here's the outline'; the buffer must be on disk before your call returns, or the worker has nothing to author. (Superseded prior wording: "When you enter finalize mode, ALWAYS Write the COMPLETE spec markdown to docs/brain/specs/{slug}.md from scratch in THIS turn before returning {'status':'finalized'} — never short-circuit with 'it already exists', becaus…") — spec-chat-db-authoring-clarity Phase 2: the stored guidance was functionally right (write the buffer this turn) but framed docs/brain/specs/{slug}.md as the artifact — an MD-based-spec framing that reads as if specs live in markdown. The DB row is the artifact (public.specs + public.spec_phases via upsertSpec); the .md is a transport scratch buffer the worker discards. Corrected wording preserves the behavior, only reframes the artifact.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `spec-chat` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Sage). The director authored this from Sage's coaching ledger; building it hardens the agent so the coaching never has to repeat.
