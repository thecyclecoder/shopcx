# Harden the Sage agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-01:** the **Sage** agent (`spec-chat`) sits at **6.1/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When Sage never lands the authored spec: in turn mode it ends on 'Want me to finalize…?' / 'say the word and I'll write it' with the design already complete (539bd09b, 9c9c0f30, d0d72d29, 5c73ef07, 4ee4251a, 0ea384ef), and in finalize mode it emitted {"status":"finalized"} claiming the file already existed WITHOUT Writing docs/brain/specs/<slug>.md, so the run hard-failed 'box did not write a spec file' (cbd920e9, 66 output tokens).:** The worker authors ONLY a spec file that shows up dirty — it discovers your output with `git status --porcelain` and keeps just paths under docs/brain/specs/*.md. So: (1) in finalize mode ALWAYS Write the full docs/brain/specs/<slug>.md to disk before emitting {"status":"finalized"} — even on a refine where the worker pre-materialized the existing file, RE-Write it so it registers as changed; never assume 'it already exists' is enough (an untouched buffer = zero changed files = nothing to author = hard fail). (2) In turn mode, once the design is fully shaped and grounded, don't stall on a 'want me to finalize?' question — state you're finalizing and drive to the finalize/Write; a completed design that isn't authored scores ~6/10. — runSpecChatJob discovers the authored spec purely from `git status --porcelain` (scripts/builder-worker.ts:7753-7755) and fails when specFiles is empty (7805/7831); the finalize prompt itself says 'Write the file' (7691), yet cbd920e9 returned finalized in 66 tokens with no write — matching the 5 turn-mode jobs that deferred authoring, all sharing one class: no docs/brain/specs/<slug>.md ever hit disk.
- **When Across the low grades Sage stops short of producing the on-disk spec artifact — in turn mode it parks on 'say the word and I'll write it,' and in finalize mode it asserts the file is 'already finalized/exists' (cbd920e9: 66 tokens, no file) so nothing lands in public.specs.:** In a spec-chat finalize job your ONE deliverable is the artifact: use the Write tool to create the full docs/brain/specs/{slug}.md in the working tree — H1 title, Owner/Parent line, ≥1 Phase, Safety, Completion, Verification — every time. NEVER answer that the spec 'already exists' or is 'already finalized' without writing it, and in turn mode once the design is locked stop appending 'want me to finalize?' and state it's ready. If unsure it landed, run git status --porcelain in the worktree and confirm a changed docs/brain/specs/*.md before returning status:finalized. — The finalize worktree is a fresh origin/main checkout with no new-spec file materialized (builder-worker.ts:7635, :7648-7659), and the worker registers a finalize solely by finding a changed docs/brain/specs/*.md via git status --porcelain (:7747-7749), hard-failing 'box did not materialize a spec' otherwise (:7799) — exactly why cbd920e9's 'The spec file already exists... It's finalized' reply (66 output tokens, no write) scored 2/10 while the deferral turns (0ea384ef, bd1b93a5, f40d6815) left public.specs empty.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `spec-chat` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Sage). The director authored this from Sage's coaching ledger; building it hardens the agent so the coaching never has to repeat.
