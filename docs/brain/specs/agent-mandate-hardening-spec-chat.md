# Harden the Sage agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-01:** the **Sage** agent (`spec-chat`) sits at **6.1/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When In finalize mode Sage declares the spec markdown 'already exists / is finalized' and skips actually writing docs/brain/specs/{slug}.md, so no file lands and nothing is authored to public.specs (job cbd920e9 hard-failed with 66 output tokens; turn-mode jobs similarly defer the write with 'say the word and I'll write it' rather than producing the artifact).:** When you enter finalize mode, ALWAYS Write the COMPLETE spec markdown to docs/brain/specs/{slug}.md from scratch in THIS turn before returning {"status":"finalized"} — never short-circuit with 'it already exists', because the finalize worktree is freshly checked out from origin/main each turn and specs live only in the DB, so the file is guaranteed absent until you create it. In turn mode, once the design is locked, produce the deliverable instead of ending on 'want me to finalize?'. — builder-worker.ts:7567 recreates the worktree from origin/main every finalize turn and no docs/brain/specs/*.md exists on main, while the finalize prompt at builder-worker.ts:7615 says 'WRITING the file ... (create it)'; job cbd920e9's 'the spec file already exists ... It's finalized' wrote zero files, so the worker's changed-file scan (specFiles from git status --porcelain) matched nothing and failed 'box did not write a spec file'.
- **When Sage narrates the intent to produce the spec — ending turns on "say the word and I'll finalize" or, in finalize mode, emitting a `{"status":"finalized"}` verdict — without ever `Write`-materializing the `docs/brain/specs/{slug}.md` scratch buffer the worker reads to author the DB row.:** When you are in finalize mode (or the founder has approved the spec's shape mid-turn), make your FIRST action a `Write` of the full spec body to `docs/brain/specs/{slug}.md`, then confirm that file exists on disk, and ONLY THEN emit `{"status":"finalized","slug":...}`. Never emit a finalized/verdict JSON from prose alone, and in a turn stop ending on "want me to finalize?" once the analysis is complete — the on-disk markdown IS the deliverable, not your message. — Failed job cbd920e9 (chat 23becaef) emitted a fenced JSON ending `...phase1-apply-fix"}` in 66 output tokens with no Write, so scripts/builder-worker.ts:7727 `if (!written)` hard-failed ("box did not write a spec file") and the chat is still status=active/spec_slug=null; the finalize path authors public.specs solely from `readFileSync(join(wt, written))` at scripts/builder-worker.ts:7734, and spec-chat SKILL.md:51 requires WRITE the buffer before the finalized JSON — the same deferred-artifact shape recurs in the turn-mode slips (f40d6815, bd1b93a5, 0ea384ef) that ended asking permission instead of producing the file.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `spec-chat` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Sage). The director authored this from Sage's coaching ledger; building it hardens the agent so the coaching never has to repeat.
