# Worker grading + director management layer ⏳

**Owner:** [[../functions/platform]] · **Parent:** [[../goals/devops-director]] (the org learns + self-manages) · hardens [[platform-director-agent]] + [[director-loop-grading]] + [[worker-coaching-loop]].

**Found in use 2026-06-23** (operator review of the live DevOps Director): she went live + autonomous but (1) only escorted goal-linked specs (missed standalone in-flight + repair-authored fix specs), (2) ran a daily beat (too slow — fixed → `*/15` in [[../inngest/platform-director-cron]]), (3) had no per-worker grading/rollup so coaching only fired on repeated errors, (4) her profile mandates read like the *function's* charter, not what she *does autonomously*, (5) the CEO had no way to coach her, and (6) her leash only auto-approves a **single plain inline action** — so a sound **multi-action bundle** (migration + its backfill) or a **multi-choice** request escalates needlessly (the `a2edeca0` / `experiment-session-stamped-attribution` punts → P8). This spec closes all of it. **CEO directive: set up to these recommendations, watch the output, tune later — so the rubrics/metrics live HERE, not in tribal memory.**

## The cascade (north star)
> **CEO** grades + coaches → **Director (Ada)** grades + coaches → **Workers (Bo, Rafa, Devi …)**

Each layer judges the layer below against an explicit rubric and coaches when the grade slips. Mirrors the existing [[../tables/director_decision_grades]] (CEO grades the director) one level down.

## Job-rating metrics (the locked config — tune in `worker-grader.ts`)
- **Gradeable unit:** one **concluded `agent_jobs` row** (a build merged, an error fixed/dismissed, an index proposed, a spec verified). The job IS the worker's atomic action.
- **Scale:** 1–10 + reasoning, `graded_by ∈ agent|human` (CEO-overridable), idempotent (one grade per job).
- **Rollup window:** **last 10** graded jobs per worker → the standing performance score.
- **Coaching trigger:** rollup **< 7/10** *or* a **drop > 1.5 pts** vs the prior 10-window → the director runs a coaching pass ([[../libraries/worker-coaching]] `coachWorker`); after N un-lifted attempts → escalate to CEO (existing loop-guard).
- **Grading cadence (batched, bounded):** run when **≥5 ungraded concluded jobs** have accumulated **OR** any are **failures** (`failed`/`needs_attention`, graded promptly) **OR** a **~3-hour** fallback. **CEO directive 2026-06-23 — don't jam the box:** a session grades **at most `GRADE_BATCH_CAP` (12)** jobs, **failures first** (the high-signal mistakes), then a **round-robin-by-worker random sample** of the successes — so chatty routine workers (fold, pr-resolve) get spot-checked without crowding out quieter ones; un-selected jobs ride a later beat. Currently runs in the deployed runtime ([[../inngest/platform-director-cron]]) over the job-row context; the Max box-session-reads-real-diffs upgrade stays a future option.

### Per-worker rubrics (what "good" = 10 means — my recommendations, calibratable via `worker_grader_prompts`)
| Worker | kind | Graded on |
|---|---|---|
| Bo | `build` | spec phases satisfied · `tsc` clean · PR merged clean (no conflict markers) · no rebuild churn |
| Rafa | `repair` | real root-cause (not symptom) · fix held (error didn't recur) · correctly dismissed noise · scoped |
| Remi | `regression` | caught a real regression · correctly dismissed flaky ones · the authored fix spec is sound |
| Devi | `db_health` | correct EXPLAIN diagnosis · the index/fix actually addresses the slow query · no foreign/sunset false-positives |
| Vera | `spec-test` | caught real drift / false-✅ · no false alarms · verification matched live prod |
| Mira | `migration-fix` | migration applied/repaired correctly · audit cleared · no data loss |
| Pax | `pr-resolve` | conflicts resolved without lost work · clean rebase · queue left mergeable |
| Fenn | `fold` | folded into the right brain pages · cross-links correct · archived cleanly |
| Cole | `coverage-register` | correct registry entry / exemption · no real coverage gap missed |
| Tao | `monitor` | accurate alerts (signal not noise) · caught real stalls |
| Pia | `plan` | sound decomposition · correct `blocked_by` · no orphan specs |
| Sol | `product-seed` | product correctly seeded · page built · orderable |
| Sage / Dex | `spec-chat` / `dev-ask` | accurate, grounded answers · correct spec edits · read-only honored |

## Phases
- **P1 — grading store + grader 🚧** *(built — migration + grader + brain pages, `tsc` clean; pending merge + apply)* — migration `worker_action_grades` (mirror [[../tables/director_decision_grades]]) + `worker_grader_prompts` (CEO-calibratable rubric, mirror `director_grader_prompts`); `src/lib/agents/worker-grader.ts` (`gradeConcludedWorkerActions` batched, `computeWorkerRollup` last-10, `detectGradeDropCoaching` >1.5-drop-or-<7 → `coachWorker`). Brain: [[../tables/worker_action_grades]] · [[../tables/worker_grader_prompts]] · [[../libraries/worker-grader]].
- **P2 — batched grading cadence 🚧** *(built — `grade-and-coach-workers` step on the `*/15` beat, `tsc` clean; pending merge)* — gate `workerGradingBatchReady` (≥5 ungraded concluded jobs OR ~3h fallback) → one batched `gradeConcludedWorkerActions` session; on the same beat run `detectGradeDropCoaching` per newly-graded worker. Wired into [[../inngest/platform-director-cron]] (deployed runtime — needs the API key, mirrors the director grade sweep; the Max box-session-reads-diffs upgrade stays a future option).
- **P3 — worker observability 🚧** *(built — `WorkerGradePanel` + `/api/developer/agents/worker-grades`, `tsc` clean; pending merge)* — each worker profile (`/dashboard/agents/[role]`) gets a **rollup-grade card** (last-10 avg + trend, the coaching signal) + a **graded-actions feed** (its recent concluded jobs each with the grade she gave it). One owner-gated fetch backs both.
- **P4 — escort in-flight + authored-fix specs 🚧** *(built — `escortFixSpecs` in the standing pass, `tsc` clean; pending merge)* — absorbs the old `director-escort-inflight-specs` gap (that spec was removed — fully covered here): in-flight specs (≥1 ✅ + ≥1 ⏳) are **already** driven by board-grooming (`findGroomCandidates` → continue/split/escalate), so the net-new lane is **0-phase repair-authored fix specs** (`Repair-signature` + platform-owned = the `error_fix` leash); a 0-phase **feature** spec (no repair signature) + new goals still escalate. Loop-guarded + dormant-until-live like the goal escort.
- **P5 — accurate autonomous mandates 🚧** *(built — `DirectorAutonomy` on her profile, `tsc` clean; pending merge)* — her profile shows what she ACTUALLY does autonomously, derived from the leash — not the function charter: auto-approves error/db/additive-migration + **migration+backfill bundles** (P8) within the leash; escorts goals + drives in-flight (grooming) + authored fix specs; grades + coaches workers; **always escalates** the rest (destructive/irreversible, new feature/goal, a choice, anything unconfirmable). Gated on her live/dormant state. [[../functions/platform]] updated.
- **P6 — real-time PM companion 🚧** *(built — `markSpecCardStatus` on every director queue/escort, `tsc` clean; pending merge)* — when she queues/escorts a spec (`escortApprovedGoals` + `escortFixSpecs`), write the [[../tables/spec_card_state]] mirror (`in_progress` + the per-phase snapshot) so the board reflects her action instantly, no markdown-deploy wait. Best-effort (never breaks the queue path).
- **P7 — CEO↔director coaching (conversational) 🚧** *(built — `director-coach` box job + `director_coach_threads`/`director_instructions`/`director_coaching_log` + injection + `DirectorCoachChat` on her profile, migration applied, `tsc` clean; pending merge. Two-button intent: **Ask** explains · **Coach her** distills a durable rule → confirmation card → injected into her decisions.)* — **CEO directive 2026-06-23: make it a two-way CHAT, not a one-way note.** The CEO asks Ada a question grounded in her *real* state — *"why haven't you started building spec X?"* — and a **resumable Max session** (the [[developer-message-center|dev-ask]] / [[box-spec-chat]] pattern, one level up, read-only over the brain + leash + roadmap + [[../tables/director_activity]] + [[../tables/agent_jobs]]) **explains** itself: X is blocked / out-of-leash / a 0-phase feature needing a CEO greenlight / loop-guarded / not yet escorted. The CEO then **coaches** in the same thread — *"build these types of specs automatically going forward"* — and Ada distills it into a durable **`director_instructions`** amendment (mirror [[../tables/worker_instructions]] + [[worker-coaching-loop]] `coachWorker`, one level up; the CEO is the gate) that's **injected into her future investigation/escort prompts** (`appendDirectorInstructions`, mirror `appendWorkerInstructions`) — so the coaching actually changes what she does autonomously next time. New box job kind `director-coach` + a `director_coaching_threads` chat surfaced on her profile; eventually the autonomous CEO drives it. Store + injection (the durable half) can land first; the chat UI second.
- **P8 — multi-action + multi-choice auto-approval (leash) 🚧** *(built — `directorLeashCandidates` multi-action gate + `additive_backfill` class, `tsc` clean; pending merge. Verified against the real `a2edeca0` actions: `apply_migration` + `run_prod_script` backfill. NOTE: true multi-CHOICE auto-decision — register-vs-exempt — was deliberately scoped OUT as riskier and not what blocked the case; multi-ACTION bundles are in.)* — **CEO directive 2026-06-23:** the director currently auto-approves only a **single, plain inline action** (`directorLeashCandidate` → `inlineApproveActionId` in [[platform-director-agent]]); a **multi-action bundle** (e.g. an additive migration **+** its dependent idempotent backfill) or a **multi-choice** approval falls through to the CEO — which is exactly why she correctly-but-needlessly punted `a2edeca0` and `experiment-session-stamped-attribution`. Generalize the gate so she MAY auto-approve a **bundle of multiple actions** and **multi-choice** requests **iff** EVERY constituent action is individually within a leash category (`LEASH_ACTION_TYPES`: `error_fix`/`db_health`/`additive_migration`/`monitoring_fix`), the bundle is **sound + reversible as a whole**, and it passes the read-only investigation verdict. A single out-of-leash / destructive / irreversible / goal-touching action in the bundle still **escalates the whole thing** — the soundness rail is unchanged, only the single-action restriction lifts. The investigation brief covers every action in the bundle (not just the first), and the verdict approves/declines the bundle atomically. Update the leash in [[../functions/platform]] + [[../goals/devops-director]].

## Verification
- A concluded `build`/`repair`/`db_health` job gets a `worker_action_grades` row (1–10 + reasoning) within one grading batch; the worker's profile shows the last-10 rollup.
- A worker whose rollup falls <7 or drops >1.5 triggers a `coachWorker` amendment (logged in `worker_coaching_log`), visible on its profile.
- Grading runs **batched** (one session per ≥5 jobs / ~3h), not one session per job.
- The director's profile mandates read as her autonomous behavior; the CEO can post a coaching note that appears in her coaching history.
- `agent-outage-resilience` P3 + repair-authored fix specs get queued by her escort (P4) without a manual queue.
