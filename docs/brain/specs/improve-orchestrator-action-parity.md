# Improve must be able to do anything to a customer the orchestrator can — via the same executor ⏳

**Owner:** [[../functions/cs]] · **Parent:** CS mandate "Ticket-derived product fixes" · **Derived-from-ticket:** `3bb28cfd-b86a-4099-a3c3-145b947e4f78`

Today Improve runs a parallel, hand-maintained executor: src/lib/improve-actions.ts implements only a subset of customer actions (the ~20 cases: partial_refund, create_return, swap_variant, remove_item, change_next_date, change_frequency, update_shipping_address, apply_coupon, skip_next_order, crisis_pause, pause_timed, pause, cancel, reactivate, update_line_item_price, marketing (un)subscribe) plus send_message, and src/lib/improve-plan-executor.ts maps plan kinds (customer_action/sonnet_prompt/grader_rule/rescore/ticket_spec/resolve_sequence) onto it. This DRIFTS from the real production surface, src/lib/action-executor.ts executeSonnetDecision, which is the single code path the orchestrator uses for ALL customer actions: action_type union (line ~18) = direct_action | journey | playbook | workflow | macro | kb_response | ai_response | escalate, where direct_action dispatches the full directActionHandler set (cancel, pause, crisis_pause, resume, reactivate, partial_refund, redeem_points_as_refund, pause_timed, apply_coupon, apply_loyalty_coupon, swap/remove/date/frequency/address/skip/price/marketing, etc.) and journey/playbook/workflow/macro route via handleJourney/handlePlaybook/handleWorkflow/handleMacro with correct per-channel delivery (incl. the now-fixed portal branch in journey-delivery.ts from #123). That divergence is exactly what produced this ticket: a journey delivered fine from the orchestrator but Improve had no way to launch one, and send_message can't email a portal customer (only emails when channel==='email'). BUILD: (1) Add an approval-gated plan-action kind 'orchestrator_action' (and/or widen 'customer_action') in the ticket-improve plan schema that carries a typed SonnetDecision {action_type, handler_name?, actions?, response_message?, reasoning}. (2) In improve-plan-executor.ts, execute that kind by calling executeSonnetDecision with an ActionContext built from the ticket (workspaceId, ticketId, customerId, channel from tickets.channel) — the EXACT production path, exactly as scripts/apply-coupon-via-executor.ts already drives it one-off — so journeys, playbooks, workflows, macros, escalate, and every direct action all work with production-correct portal/email/chat/sms delivery. (3) Migrate the existing hand-rolled improve-actions.ts customer cases to delegate to the shared executor (or thin wrappers over it) so there is ONE customer-action code path, killing future drift (aligns with CLAUDE.md North star: a supervised role-agent driving the bounded executor tool, and the 'identical ticket messages' invariant). (4) Surface the decision + reasoning on the approval card (North star: the tool must show its reasoning and respect guardrails — hitting a rail escalates, not executes), keep everything gated behind pending_actions/approval, and keep co-sign rules for high-blast-radius actions. (5) Update .claude/skills/ticket-improve/SKILL.md action shapes, docs/brain/specs/box-ticket-improve.md, and docs/brain/lifecycles/ai-analysis.md + orchestrator-tools.md to document the expanded surface. Verify by sending Krystin the cancel_subscription journey from Improve on ticket 3bb28cfd and confirming the CTA lands in the portal thread, is emailed via sendPortalThreadEmail, and shows in the conversation window.

## Problem (from ticket `3bb28cfd-b86a-4099-a3c3-145b947e4f78`)
The CX co-pilot's action scope is narrower than the orchestrator's because Improve uses a separate, partial executor (improve-actions.ts) instead of the production one (action-executor.ts executeSonnetDecision). The founder cannot ask Improve to launch a journey, run a playbook/workflow/macro, escalate, or use any direct action outside the hand-maintained subset, and even shared actions can drift (e.g. send_message doesn't email portal customers). On this ticket that meant the cancel journey could not be sent to the customer from Improve at all despite the delivery path being fixed.

## Phases
- ⏳ **P1 — implement the fix** — scope from the problem above; land code + a brain page; gate on `npx tsc --noEmit`.

## Verification
- Reproduce the ticket scenario → confirm the fixed behavior, and that the ticket that surfaced it would now be handled correctly.

> Authored by the box Improve agent from ticket `3bb28cfd-b86a-4099-a3c3-145b947e4f78`. Commission the build from the Roadmap board (owner = cs).
