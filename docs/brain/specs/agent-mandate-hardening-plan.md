# Harden the Pia agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-01:** the **Pia** agent (`plan`) sits at **5.7/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When In all four passes Pia concluded the plan job without first doing the brain-grounded gap analysis — twice emitting 'no proposals' with no documented reasoning, twice emitting a decomposition with empty/flat blocked_by built on an unverified premise.:** Never conclude a plan pass until you've done the Procedure-step-2 brain-grounded gap analysis and can document the result. If there are no gaps, return {status:'completed',summary:'<why, grounded in the brain page you checked>'} — never a bare completed, which logs verbatim as 'no proposals' (builder-worker.ts:6335) and is indefensible against the rubric (SKILL.md:47 requires the summary). If there are gaps, first verify every premise against the actual integration/table brain page before decomposing (SKILL.md:15 — e.g. read the AdLibrary integration page instead of assuming it can't yield destination URLs), then wire each spec's blocked_by from its real upstream deps (SKILL.md:32); a flat blocker-less multi-milestone tree is auto-rejected and re-planned (isFlatBlockerlessTree, builder-worker.ts:6304). — The two 'no proposals' jobs (mqqwxdie, mqqvwglo) returned completed with no summary, which builder-worker.ts:6335 logs as the literal 'no proposals' the grader penalized; the two superseded jobs left blocked_by empty — storefront-optimizer tripped the isFlatBlockerlessTree re-plan at builder-worker.ts:6304 and acquisition mqqvp52y rested on a false premise SKILL.md:15 forbids — so a single fix, ground-and-document before concluding, closes all four.
- **When Pia ends plan passes without the mandatory grounding+self-sequencing — twice returning a null 'no proposals' output and twice emitting a multi-spec tree where every spec has blocked_by:[] .:** At the START of every plan job, ground before deciding the output: read the goal + brain, VERIFY each capability premise (e.g. confirm what AdLibrary actually returns before asserting it doesn't), then emit a self-sequenced tree where every spec that consumes another's output declares its real prerequisites in blocked_by. Never terminate with a bare {"status":"completed"} (no summary) or a tree where all blocked_by are empty — if you genuinely cannot decompose, escalate with {"status":"needs_input"} stating the concrete blocker; if milestones truly are independent, say so explicitly in each spec's intent. — builder-worker.ts:6335 writes the 'no proposals' sentinel only when the planner returns completed with no summary (mqqwxdie, mqqvwglo), and isFlatBlockerlessTree at :6196-6198 forces the supersede re-plan at :6304 whenever no spec sets blocked_by (mqqvp52y, storefront mqpf7t9d) — both sinks are avoided by doing the :6277 self-sequencing and using the :6290 needs_input escalation instead of a null/flat exit.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `plan` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Pia). The director authored this from Pia's coaching ledger; building it hardens the agent so the coaching never has to repeat.
