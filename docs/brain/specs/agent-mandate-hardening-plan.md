# Harden the Pia agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-01:** the **Pia** agent (`plan`) sits at **5.7/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When Pia reasons about the decomposition in prose but fails to emit the machine-readable deliverable — two plans bailed to status:completed / 'no proposals' with no tree, one wrote the dependency graph in `intent` prose with an absent `blocked_by` field, and one left `blocked_by` empty.:** When you finish planning a goal, your FINAL message must be a `needs_approval` tree where every spec that another spec depends on lists those slugs in its structured `blocked_by` array — before returning, scan each spec's `intent`/`gap` for any 'blocked by / after / consumes / needs X first' phrase and lift it into that spec's `blocked_by` field (never leave the dependency only in prose, never leave `blocked_by` empty when you named a prerequisite). Never conclude `status:completed`/'no proposals' when the goal still has undecomposed milestones; if you genuinely propose nothing, you MUST put the reason in `summary`. And verify an integration's real capability against its source in src/lib before letting a claimed limitation shape the tree. — In superseded job e0360ca2 the proposed specs smqpfg33a4 ('blocked by M1, M2, predicted-ltv-per-visitor-metric') and smqpfg33a5 ('blocked by storefront-optimizer-agent') carry NO `blocked_by` field at all while job 22d33c39's error is literally 'blocked_by deps were empty', and jobs 9942f2d6 & c0c237bd returned log_tail 'no proposals' with pending_actions=[] despite instructions listing the exact 5 specs + edges — forcing a non-null tree with populated `blocked_by` gives isFlatBlockerlessTree/self-sequencing real edges instead of triggering the supersede/re-plan churn the rubric penalizes.
- **When Pia ends the planning pass before completing the mandated grounding — twice emitting a bare 'no proposals' null with no justifying summary (on a goal that in fact had gaps and was later re-decomposed), and twice emitting an ungrounded decomposition (a false external-capability premise plus empty blocked_by) that the worker had to supersede/re-plan.:** Before you emit your final JSON on ANY plan job, finish the grounding the plan-goal skill requires: (1) actually gap-analyze against the brain — never conclude 'no proposals'; if you truly find no gap, you MUST return status:completed WITH a summary citing the brain page that proves the goal is already covered, never let it fall through to the bare 'no proposals' default; (2) VERIFY every premise about an external capability (e.g. 'AdLibrary doesn't give destination URLs') by reading its integrations/ brain page — never assert it from memory; (3) for every proposed spec trace its real build-order deps and populate blocked_by with prerequisite slugs — leave [] only after confirming genuine independence and say so in intent. A conclusion (null or flat) that skips these is what gets your plans graded as null-output failures or superseded. — The two 'no proposals' outcomes hit the summary-less default at builder-worker.ts:6409 (SKILL.md:47 requires a justifying summary) yet were false nulls — mqqvp52y's 're-decomposing' error proves gaps existed; the two supersedes tripped isFlatBlockerlessTree at builder-worker.ts:6270-6272 / re-plan 6378-6404 because blocked_by was empty (SKILL.md:32) and rested on an unverified AdLibrary premise (SKILL.md:22). Forcing brain-grounding + premise-verification + blocked_by-mapping before concluding removes both failure modes at their shared root.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `plan` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Pia). The director authored this from Pia's coaching ledger; building it hardens the agent so the coaching never has to repeat.
