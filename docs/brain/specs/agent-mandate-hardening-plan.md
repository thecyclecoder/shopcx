# Harden the Pia agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-01:** the **Pia** agent (`plan`) sits at **5.7/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When Across all four jobs Pia emitted a terminal plan output — either a bare "no proposals"/completed (mqqwxdie, mqqvwglo) or a decomposition resting on an unverified premise with empty blocked_by (mqqvp52y's false "AdLibrary doesn't give destination URLs", mqpf7t9d's flat blocker-less tree) — without first running the SKILL.md:22 brain-grounding pass that verifies the goal's premises and yields the real dependency edges.:** Before you emit ANY terminal status on a plan job, first execute plan-goal Procedure step 2 (SKILL.md:22): read the brain pages for the success metric and build the explicit two-column 'what exists (cite the page) vs what's missing (gap)' model, and verify every capability premise the goal states against its actual integration/library brain page (e.g. check docs/brain/integrations/adlibrary.md before asserting AdLibrary can't yield destination URLs). Derive blocked_by directly from that map — every spec that consumes another's output lists its prerequisite slug; only use blocked_by:[] with an explicit 'independent — no build-order dependency' note in that spec's intent. Never return {"status":"completed"}/"no proposals" unless you cite the brain pages proving every milestone is already covered. — The two supersedes died on empty blocked_by — a flat tree caught by isFlatBlockerlessTree (scripts/builder-worker.ts:6264-6266) and a premise refuted by docs/brain/integrations/adlibrary.md that grounding would have read (SKILL.md:15) — while the two null outputs took the bare 'no proposals' escape (scripts/builder-worker.ts:6403, SKILL.md:47); grounding first turns unverified assumptions into real gaps and the dependency map into correct blocked_by edges, fixing all three rubric criteria at once.
- **When Pia emits a terminal planning decision — a null 'no proposals'/completed or a spec tree — before grounding it: she asserts current-state/capabilities without opening the brain page that would confirm or refute them, and leaves blocked_by empty.:** Before returning ANY terminal status, do the grounding pass first: for every current-state or capability claim, open the exact brain page and confirm it (e.g. read docs/brain/libraries/adlibrary.md before claiming AdLibrary lacks destination URLs), then set each spec's blocked_by from the real build-order edges you found. Never return completed/'no proposals' without a summary citing the brain page that proves no gap remains, and never return a multi-spec tree with every blocked_by empty. — adlibrary.md:19 already documents `destination_domain` ('the store domain this ad drives to') + `has_store_url`, directly refuting mqqvp52y's false premise — grounding per SKILL.md:22 would have caught it; the two 'no proposals' jobs hit builder-worker.ts:6335 (completed, no summary) despite the goal needing work, and the storefront job tripped the empty-blocked_by re-plan gate at builder-worker.ts:6304 — all one skipped grounding+dependency pass.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `plan` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Pia). The director authored this from Pia's coaching ledger; building it hardens the agent so the coaching never has to repeat.
