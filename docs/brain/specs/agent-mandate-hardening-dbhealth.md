# Harden the Devi agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-01:** the **Devi** agent (`db_health`) sits at **4.6/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When Two lowest-graded findings (tickets 4ms×1.27M, sms_campaign_recipients 31ms×157K) were both diagnosed bloat_stale_stats → vacuum_tuning on fast-mean/high-stddev queries whose real lever was a missing index or call-volume, and both were dismissed by the owner as 'vacuum won't help.':** When a slow query has a low mean (< ~50ms, i.e. SLOW_PER_CALL_MEAN_MS) OR high stddev but the EXPLAIN plan isolates no Seq Scan, NEVER default to bloat_stale_stats/vacuum_tuning — diagnose it as high_call_volume (reduce calls / cache / widen poll) or a missing hot-predicate index, and pick a GIN index when the predicate is an array/JSONB containment (`@>`, e.g. tickets.tags). Only propose vacuum_tuning when there is a concrete dead-tuple/autovacuum-stale signal (BLOAT_DEAD_RATIO_FLAG + stale autovacuum), not merely because the plan 'looks off.' — db-health.ts:288 makes bloat_stale_stats the fallback whenever classifyExplainPlan finds no Seq Scan, which is exactly how the sub-50ms/high-stddev tickets and sms queries were misrouted to vacuum; the correct branch already exists at db-health.ts:312-325 (mean < SLOW_PER_CALL_MEAN_MS → high_call_volume, line 320 `@>` → GIN), matching the owners' 'call-volume / GIN on tags, not bloat' dismissals.
- **When Devi concludes a db_health job — authoring the fix spec / queuing the build — without the EXPLAIN plan, exact stats, and exact DDL ever materializing into the spec body or log_tail, so the action reads as an ungradeable guess even when the diagnosis is correct.:** When you conclude any db_health slow-query job, do NOT leave the spec body or log_tail as a bare 'queued build' — before you queue/conclude, write the full evidence INLINE into the artifact: the verbatim EXPLAIN (ANALYZE, BUFFERS) plan with the named bottleneck node and its 'Rows Removed by Filter', the exact pg_stat_statements query text with calls/mean/stddev/total, the one-line diagnosed cause, and the exact fix (the literal CREATE INDEX CONCURRENTLY / rewrite / reduce-calls change). Assert the spec body is non-empty before it lands. Do this because a proposal whose diagnostic didn't materialize is dismissed or capped at ~5/10 regardless of correctness — the grader can only score what's surfaced. — The db-index-orders spec was authored as a 0-byte file (commit 4362025a: 'docs/brain/specs/db-index-orders.md | 0') and had to be recovered by hand (fac13e78: 'the db-health agent's auto-authored spec body came up empty (agent bug)'), which then supplied the EXPLAIN Seq Scan (Rows Removed by Filter: 125137) and the exact orders(workspace_id, created_at DESC) index; the two -7821780334453251234 slips left log_tail as 'queued build' even though build #374 (0f6e08cc) later documented the correct high_call_volume diagnosis — matching every grade=5 grader's 'no EXPLAIN output, sparse log' complaint.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `db_health` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Devi). The director authored this from Devi's coaching ledger; building it hardens the agent so the coaching never has to repeat.
