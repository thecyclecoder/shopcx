# Harden the Cole agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-01:** the **Cole** agent (`coverage-register`) sits at **5.7/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When When materializing the owner-approved coverage-register fix spec, the worker authored it as a docs/brain/specs/{slug}.md git commit that failed, then parked as needs_attention with no diagnosis — leaving the registry entry uncreated and the coverage gap open.:** When you materialize an approved fix spec (register OR exempt), author it through the specs-table SDK — markNewSpecInReview → authorSpecRowFromMarkdown → upsertSpec into public.specs — NEVER by writing or committing a docs/brain/specs/{slug}.md file (CLAUDE.md hard rule: PM writes go through the SDK, not a .md fetch/commit). And if the author call throws, write the concrete SDK error + the remediation path into error/log_tail and escalate — do not conclude needs_attention with an empty diagnosis. — The graded job's log_tail 'failed to author docs/brain/specs/register-loop-acquisition-research-cadence-cron.md' is the .md-commit path failing; the SDK path at scripts/builder-worker.ts:2703 → author-spec.ts:6107-6110 (upsertSpec → public.specs) never touches a .md file, so routing through it removes the exact failure that left the gap unresolved.
- **When When a coverage-register job cannot land the registry entry (spec-commit/authoring failure or missing/empty spec body) it concludes with a terse one-line needs_attention error and returns immediately — no diagnostic summary of what failed, no retry of the spec authoring, and no escalation carrying a remediation path.:** When you cannot materialize the registry entry/exemption, never park with a bare needs_attention/needs_approval and stop. First write a diagnosis (which step failed — markNewSpecInReview / build enqueue / missing spec body — and why), then retry the spec authoring once, and if it still fails, escalate with a concrete remediation path (surface the already-inferred entry snippet from instructions.entry via renderEntrySnippet for one-tap/manual paste). The concluded job must always carry visible reasoning + a next action, because a coverage gap left with no explanation reads as unresolved-and-abandoned. — Every failure branch in runCoverageRegisterJob (scripts/builder-worker.ts:2685, :2699, and the no-decision park at :2724) sets a terse status+error and `return`s with no summary/retry/escalation — so the 3/10 job's 'spec commit failure' concluded needs_attention with nothing for the grader to see, exactly the 'no summary or evidence of fallback reasoning' the rubric flagged; the inferred entry needed to retry/escalate is already sitting in instructions.entry (coverage-register-agent.ts:284-293), so surfacing it is cheap.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `coverage-register` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Cole). The director authored this from Cole's coaching ledger; building it hardens the agent so the coaching never has to repeat.
