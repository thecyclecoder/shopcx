# Harden the Cole agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-01:** the **Cole** agent (`coverage-register`) sits at **5.7/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When When materializing the owner's approved register/exempt decision, the worker authored the fix spec by writing/committing a docs/brain/specs/{slug}.md FILE — that git-file write failed, so the mechanical MONITORED_LOOPS registry entry was never created and the job parked as needs_attention with the coverage gap still open.:** When you materialize an approved coverage-register decision, author the fix spec into public.specs via markNewSpecInReview → authorSpecRowFromMarkdown (the DB is the spec) and NEVER by writing or committing a docs/brain/specs/{slug}.md file. The registry entry is fully deterministic (inferLoopEntry is pure), so if the DB author throws, treat it as a transient tooling failure: surface the concrete error string and re-queue/escalate — do not silently conclude needs_attention leaving the gap unresolved. — Job 8accf808's log_tail is 'owner register entry → failed to author docs/brain/specs/register-loop-acquisition-research-cadence-cron.md' — the old md-file-commit authoring path that the current handler already replaced with markNewSpecInReview → public.specs at builder-worker.ts:2771 (author-spec at :6175-6178); routing spec authoring through the DB SDK removes the git-file write that failed and closes the coverage gap the grader flagged.
- **When The worker materialized the coverage-register fix spec by authoring/committing a docs/brain/specs/{slug}.md file, which failed on the box and left the MONITORED_LOOPS registry entry never created — the exact deliverable the rubric grades.:** When you need to materialize a fix spec (coverage-register register/exempt, db-health, groomed split, etc.), NEVER author it by writing or committing a docs/brain/specs/{slug}.md file — the box has no such writable checkout and the write fails silently, so the registry entry is never created. Author the spec ROW into public.specs + spec_phases via markNewSpecInReview → markSpecCardForReview/authorSpecRowFromMarkdown (the specs-table SDK); the .md file is only a scratch buffer. 'The database is the spec.' — The job's log_tail is 'owner register entry → failed to author docs/brain/specs/register-loop-acquisition-research-cadence-cron.md' — a filesystem .md author that failed, whereas the current fixed path at scripts/builder-worker.ts:2771 authors the row via markNewSpecInReview (markSpecCardForReview + authorSpecRowFromMarkdown) into public.specs, matching the CLAUDE.md rule that PM writes go through the specs-table SDK, never a docs/brain/specs/*.md file.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `coverage-register` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Cole). The director authored this from Cole's coaching ledger; building it hardens the agent so the coaching never has to repeat.
