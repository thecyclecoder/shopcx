# Harden the Cole agent — roll persistent coaching into its mandate

**Owner:** [[../functions/platform]] · **Parent:** [[platform-director-agent]] — graduate persistent coaching into a durable fix (director grades agents: low score → fix spec, never a CEO escalation)
**Found in use 2026-07-01:** the **Cole** agent (`coverage-register`) sits at **5.7/10** after **2** coaching attempts that didn't stick. Rather than escalate to the CEO, roll the coaching into a permanent fix so the agent improves at the mandate level.

## The accumulated coaching to bake in (now archived as rolled-into-mandates)
- **When When materializing the chosen fix spec, the coverage-register runner lets markNewSpecInReview throw uncaught, so an authoring failure escapes to the generic backstop and parks a bare, reasonless needs_attention instead of a classified, diagnostic one.:** When you materialize a coverage-register fix spec, wrap the markNewSpecInReview + build-enqueue block (builder-worker.ts:2703-2714) in try/catch and, on failure, park needs_attention with a specific needs_attention_class + a diagnostic error/log_tail (e.g. `spec authoring failed for <slug>: <err.message>`) — the SAME classified-park pattern the sibling pre-checks at builder-worker.ts:2685 and :2699 already use. A MissingVerificationError means the generated body lost its `## Verification` block, so name that; never let the throw fall through to the 'produced no verdict' backstop, and re-attempt or escalate meaningfully so the coverage gap doesn't silently stay open. — The pre-check branches at builder-worker.ts:2685/:2699 write error+log_tail before parking, but the authoring call at builder-worker.ts:2703 is unguarded and markNewSpecInReview rethrows MissingVerificationError (author-spec.ts:6117/:221) into the reasonless park path (builder-worker.ts:1540) — matching the grader's 'no diagnosis, no retry, gap unresolved'; catching+classifying there is the exact fix.
- **When Coverage-register entries land carrying inferOwner's fallback owner:"platform" plus the boilerplate "Confirm the owner-function" placeholder instead of the loop's real owner, so the entry is 'created' but not correct.:** At the START of every coverage-register job, before authoring, check whether the loop's domain is actually matched by inferOwner's narrow regexes (coverage-register-agent.ts:104-108). If it falls through to the owner:"platform" default (line 108) — as acquisition-research-*, creative-finder-*, and other research/ads-adjacent crons do — do NOT accept that guess or ship the 'Confirm the owner-function' placeholder description (line 119). Instead open the loop's inngest module header / parent goal and set the true owner (acquisition-research-cadence-cron is M5 of the Acquisition Research Engine → growth; creative-finder-* siblings are owner:"growth"), then author the entry with that owner and a real label/description. — registry.ts:504-512 and :527-534 both landed with owner:"platform" and the literal "Confirm the owner-function" placeholder, yet acquisition-research-cadence.ts:3-4 shows the cron is a growth-owned Acquisition Research Engine loop and its creative-finder sibling is owner:"growth" (registry.ts:526) — so inferOwner's line-108 default silently mis-owns these entries; deriving the owner from the module/goal instead of the id-regex guess makes the entry pass the rubric's 'correct registry entry' bar rather than merely existing.

## Phase 1 — bake the coaching into the agent
- Make the above coaching PERMANENT behavior of the `coverage-register` agent — fold it into its prompt/mandate/code (its run-job + prompt in `scripts/builder-worker.ts` and the relevant `src/lib/agents/*`), not ephemeral appended `agent_instructions`. Once baked, the agent should follow it by default.
- Verify the agent's grade rollup recovers (≥ 7/10) over the next window of graded actions; if a coaching class still recurs, the bake-in missed it.

## Ownership
Owner: [[../functions/platform]] (Ada supervises Cole). The director authored this from Cole's coaching ledger; building it hardens the agent so the coaching never has to repeat.
