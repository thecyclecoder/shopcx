# director_activity

The timestamped **action log** every director (and every worker a director supervises) writes a row to on each action it takes ([[../goals/devops-director]]). That single log is the substrate for **(1)** the autonomous-approval **audit history**, **(2)** the gamified [[../libraries/director-board|#directors board]] posts, and **(3)** the **EOD recap** — a read over *today's* rows, never hand-maintained.

Written via [[../libraries/director-activity]] `recordDirectorActivity` (best-effort, service-role only). The **first concrete writer** is the [[../specs/regression-agent|Regression Agent]] (a worker the Platform/DevOps Director supervises): every detect / dismiss / author / escalate action writes one row. The live directors (M4+) write the same shape (`approved_migration`, `fixed_bug`, `escorted_goal`, …).

**Workspace-scoped** (mirrors [[director_messages]] / [[spec_card_state]]). RLS: any authenticated user reads (the board + recap surfaces are owner-gated above the DB); service role does all writes.

**Migration:** `supabase/migrations/20260702120000_director_activity.sql` · apply via `npx tsx scripts/apply-director-activity-migration.ts`.

**Primary key:** `id`

## Columns

| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` | PK · `gen_random_uuid()` |
| `workspace_id` | `uuid` | FK → `workspaces(id)` on delete cascade |
| `director_function` | `text` | the function slug whose objective owns the action (e.g. `platform`); a **worker** action carries its **supervising director's** function |
| `action_kind` | `text` | what was done — **open vocabulary, no CHECK** (new kinds land without a migration). regression-agent emits: `detected_regression｜dismissed_regression｜authored_fix｜escalated`; the [[../libraries/agent-coaching|coaching pass]] emits: `coached_worker｜coaching_routed_to_repair｜escalated_coaching`; the [[../libraries/platform-director|Platform/DevOps Director]] emits: `approved_approval｜escorted_goal｜escorted_fix｜escorted_init｜escalated｜reconciled_error` (the last = [[../specs/director-zero-backlog-error-autonomy]] Phase 1 — one row per backlog action: an enqueued repair diagnosis or a stuck-fix escalation, `metadata: { signature, source, action: 'enqueued_repair'｜'escalated_stuck' }`) + `reconciled_sequence` ([[../specs/goal-milestone-build-sequencing]] Phase 3 — one row per out-of-order milestone build re-sequenced: the premature build held at the `held` status + the authored `**Blocked-by:**` order applied to the spec file, `metadata: { goal_slug, held_job_id, held_from_status, held, declared_blockers, unmet_blockers, applied_blockers }`) + `triaged_needs_attention` ([[../specs/needs-attention-triage-and-verdict-robustness]] Phase 1 — one row per triaged NON-build parked item: a recoverable inconclusive QC verdict re-run once or a genuine blocker surfaced to the CEO, `metadata: { job_id, kind, action: 'rerun'｜'escalated', reason, dedupe_key }`) + (board-grooming) `groomed_continue｜groomed_split` + (board-grooming richer verdicts [[../specs/director-judgment-lanes-fold-author-dismiss]]) `groomed_fold_now｜groomed_authored_spec｜groomed_dismissed｜init_authored_spec｜init_dismissed｜repair_authored_spec` ([[../specs/director-supervised-repair-dismissal]] — supervising Rafa's no-fix items) `dismissed_repair｜kept_repair` (`metadata: { dismiss_key, repair_job_id, signature, title, verdict }`; Phase 2 adds `reopened_repair` — written by `POST /api/developer/control-tower/repair {action:'reopen'}` when the owner overrides a dismissal, `metadata: { repair_job_id, signature, requeued, reopened_by }`, which clears the item from the Control Tower's Director-dismissed surface); a director proposing a goal emits `proposed_goal` ([[../specs/director-proposed-goals]] — written by `scripts/builder-worker.ts` `runProposedGoalJob` when the inert `proposed` artifact is committed, `metadata: { goal_slug, owner_function, proposer_function }`); [[../specs/director-dismiss-park-and-short-circuit-spec]] Phase 1 — a director's `dismiss-park` action emits `dismissed_park` (`metadata: { job_id, spec_slug, prior_class, target_kind, autonomous: true }`); the owner-gated `POST /api/developer/director-activity/reopen-park` re-open emits `reopened_park` (`metadata: { job_id, target_kind, spec_slug, reopened_by: 'owner' }`); a malformed dismiss-park payload (missing jobId / reason, out-of-leash owner, row not parked) emits `invalid_dismiss_park_action` instead. Phase 2 — a director's `spec-status` action carrying `shortCircuit:true` emits the standard `spec_status_flipped` row with `metadata.short_circuit=true` so the activity feed surfaces the short-circuit transition. CEO bouncing director escalations back ([[../specs/bounce-escalation-back-to-director]] / [[../libraries/director-bounce-back]]) emits `bounced_back_by_ceo` (`metadata: { lane, original_notification_id, original_escalation_reason, ceo_note?, new_job_id, depth:1 }`); a director re-escalating after a bounce emits `re_escalated_after_bounce` (`metadata: { lane, depth:2, original_notification_id, bounce_job_id, diagnoses:[original, post_bounce] }`). **re-author-re-opens-dismissed** — `[[../libraries/director-activity]] clearDirectorSpecDismissals` (called by `[[../libraries/author-spec]] reopenIfReauthoredAndChanged`) emits `spec_reopened_after_reauthor` (one row when a RE-AUTHORED spec's standing `init_dismissed`/`groomed_dismissed` dedup rows are DELETED so the corrected content re-enters review→build instead of sitting dead under the stale verdict, `metadata: { cleared_dismissals, cleared_keys, autonomous:true }`). The [[../libraries/deploy-guardian|Deploy Guardian]] emits `deploy_healthy｜deploy_regressed｜deploy_unsure｜deploy_rolled_back` (one per evaluated [[deploy_watches]] canary window — `deploy_rolled_back` = a regressed deploy auto-reverted to the prior good build (Phase 2), `metadata: { deploy_watch_id, branch, merge_sha, verdict, new_error_signatures, new_red_loops, revert_sha?, prior_rollbacks?, loop_guard?, revert_failed?, ... }`). The Growth director's storefront-experiments refresh emits `detected_undelivered_experiment` ([[../libraries/storefront-experiment-delivery-audit]] Phase 2 — one row per undelivered `running`/`promoted` experiment detected by the audit (≥1 of: sessions + exposure events in the last 24h), `director_function='growth'`, `metadata: { experiment_id, lander_type, hours_since_start, sessions_count, exposures_count }`, used to surface blocked experiments and hold the bandit pending Director review). Growth director's ready-to-test promotion ([[../specs/growth-adopt-creative-makers]]) emits `promoted_ready_to_test` (one row per Director-approved creative promotion to the publish flow, `metadata: { ad_campaign_id, ad_publish_jobs_id, meta_ad_id? }`) and `attributed_creative_outcome` (one row per published creative's attributed ROAS outcome ≥3d after publish, `metadata: { ad_publish_jobs_id, meta_ad_id, attribution_window_days:7, outcome:{roas?, spend_cents, sessions, variant_key?} }`). The Platform/DevOps Director (Ada) also receives a **human-only-promote advisory** `human_only_promote_advisory` ([[../libraries/director-activity]] `recordHumanOnlyPromoteAdvisory`, spec-test-human-only-promote-gate sub-task 1b — one row when a ZERO-machine-coverage spec, `summary.auto_pass===0`, promotes at the M4 auto-merge point [[../libraries/github-pr-resolve]]; LIGHTWEIGHT + NON-BLOCKING + idempotent, `director_function='platform'`, `metadata: { autonomous:true, advisory:true, zero_machine_coverage:true }`). The **spec-drift reconciler** ([[../libraries/spec-drift]], `actor:'reconciler:spec-drift'`, `director_function='platform'`) emits `healed_built_unstamped` (stamped a built-but-unstamped phase shipped, `metadata: { pr, merge_sha, phases }`), `spec_phases_anomaly` (surfaced orphan / duplicate-position / provenance-gap rows), and `reconciled_archived_not_folded` (`folded-spec-must-stay-folded` — re-persisted `folded` on a spec whose markdown is in `docs/brain/archive.d/` but whose DB row had drifted to a non-folded status, `metadata: { signal:'archived-not-folded', previous }`). Ada's **spec-drift supervision lane** (`scripts/builder-worker.ts` `runSpecDriftSupervision`, [[../specs/ada-standing-pass-reasoning-gate]] Phase 1, `director_function='platform'`) emits `drift_supervised` — one row per terminal verdict on a `spec_drift` row (from the deterministic pre-filter OR the fallback Max session) so the ledger dedup (`alreadyDriftSupervised`, keyed on `metadata.drift_row_id`) can skip the row on the next pass instead of re-spawning a session forever on an unresolved open row (a `code-missing`/`unsure` verdict keeps the row open, and the reconciler creates a NEW row.id when re-detecting, so an unchanged open row stays skipped while a re-surfaced case is re-investigated). `metadata: { actor:'drift-supervise:pre-filter'|'drift-supervise:session', drift_row_id, verdict:'code-present'|'code-missing'|'unsure', source:'pre-filter'|'session', symbols_found?, paths_missing? }`. The **completed-goal self-archive reconciler** ([[../libraries/agent-jobs]] `reconcileCompletedGoalsToFolded`, `actor:'reconciler:completed-goal-self-archive'`, `director_function='platform'`) emits `reconciled_completed_goal_folded` (folded a COMPLETE non-parent goal whose 100% rollup shipped one-off so the M5 atomic-promote path never retired it — ran `finalizePromotedGoal`; PARENT goals — `is_parent` OR has child goals incl. folded children OR no buildable specs — are NEVER folded; `spec_slug` carries the goal slug, `metadata: { signal:'completed-goal-not-folded', linked_spec_count, milestones, completed, fold_queued }`). [[../specs/ceo-authorized-out-of-leash-actions]] Phase 1 — Ada's [[director-coach-threads|Ask-Ada chat]] emits `raised_out_of_leash_request` (one row per founder-prompted out-of-leash Approval Request she raised for the CEO to decide: she investigated read-only, INDEPENDENTLY AGREED it's sound + the right call, and raised a CEO-routed `agent_jobs` row (`kind='ceo-authorized-out-of-leash'`, `status='needs_approval'`) carrying her reasoning + a concrete executable pending-action; `metadata: { target_job_id, thread_id, action_type, reversibility, irreversible, leash_rail, founder_ask, autonomous:false }`; a malformed payload emits `invalid_out_of_leash_request_action` instead). Phase 2 — `runCeoAuthorizedOutOfLeashJob` (`scripts/builder-worker.ts`) emits `executed_ceo_authorized_out_of_leash` on the CEO-approved shell-execute of the concrete `cmd` (SCOPED, ONE-TIME, `authorized_by='ceo'`-stamped; the leash config is NEVER modified), `metadata: { target_job_id, thread_id, action_type, action_id, cmd, reversibility, irreversible, authorized_by:'ceo', outcome:'ok'|'failed', result_tail, autonomous:false }`; and `ceo_declined_out_of_leash_request` on a CEO decline (no execution ran, matching audit shape to the approve path), `metadata: { target_job_id, thread_id, action_type, action_id, cmd, reversibility, irreversible, authorized_by:'ceo', autonomous:false }`. The **Media Buyer** ([[../libraries/media-buyer-agent]], `director_function='growth'`) emits `media_buyer_promoted_winner｜media_buyer_paused_loser｜media_buyer_reactivated_recovered｜media_buyer_replenished_test_cohort｜media_buyer_fatigue_replenish_triggered｜media_buyer_replenish_missing_config｜media_buyer_replenish_no_diverse_candidate｜media_buyer_no_active_policy｜media_buyer_sensor_trust_denied｜media_buyer_pass_completed` — one row per plan action + a pass heartbeat. **`media_buyer_replenish_no_diverse_candidate`** ([[../specs/dahlia-andromeda-concept-diversity-tags]] Phase 2 — one row per pass where the replenish loop partialed because every remaining ready-to-test candidate's Andromeda `concept_tag` was already represented in the live cohort, so the plan under-filled the deficit to preserve concept diversity, `metadata: { cohort_id, product_id, live_concept_tags, ready_tags_available, cohort_target_count, current_test_cohort_size, replenish_filled, autonomous:true }`; surfaced to Growth so Dahlia can author fresh concepts) — the tag data is CITED, not paraphrased. The **CS Director (June)** coaching loop emits `sol_messy_turns` ([[../libraries/sol-coaching-signal]] `recordSolMessyTurns`, `director_function='cs'`) — one row per ticket whose ENDING was satisfactory (no escalation) but whose MIDDLE had a RECOVERED stumble, raised by BOTH Cora tiers (the cheap Haiku triage pass on a clean-close ticket + Cora's deep session on a no-escalation verdict), `metadata: { ticket_id, tier:'cheap'|'cora', signals:[recovered-stumble classes], score }`. June's weekly digest ([[../libraries/cs-director-digest]] `composeMessyTurnWarnings`) groups these by signal class across DISTINCT tickets and surfaces an `early_warning` + `add_rule` fix once a class recurs ≥3 tickets. |
| `spec_slug` | `text` | the spec the action touched (null for a non-spec action) |
| `reason` | `text` | the plain-text "why" — the reasoning the recap/audit reads back · default `''` |
| `metadata` | `jsonb` | structured per-action context: `{ job_id?, signature?, failing?, attempt?, verdict?, approver?, owner_function?, ... }` · default `{}` · **`owner_function`** ([[../specs/director-drives-all-specs-and-deferred-status-board-reflects-cross-dept-drive]] Phase 1) — the OWNING function of the spec the action touched, stamped by every Platform-director drive lane that may cover for a not-yet-live department (`escorted_fix｜escorted_init｜groomed_continue｜groomed_split｜groomed_fold_now｜groomed_authored_spec｜groomed_dismissed｜init_authored_spec｜init_dismissed`); the daily Platform watch counts rows where `owner_function ≠ 'platform'` (= the keystone covering cross-dept) and names them per-owner ("Drove K specs for other departments (cs: 2, growth: 1)"). Repair-lane and approval-lane rows don't carry it (no spec owner / it's an approval, not a drive). |
| `created_at` | `timestamptz` | default `now()` (the EOD recap filters `created_at >= today`) |

## Indexes

- `director_activity_ws_created_idx` on `(workspace_id, created_at desc)` — the recap/audit read.
- `director_activity_function_idx` on `(director_function, created_at desc)` — per-director slice.
- `director_activity_spec_idx` on `(spec_slug)` — per-spec audit slice.

## Related

[[../libraries/director-activity]] · [[../specs/regression-agent]] · [[../specs/security-dependency-agent]] · [[../libraries/security-agent]] · [[../goals/devops-director]] · [[../specs/director-loop-grading]] · [[../specs/worker-coaching-loop]] · [[../libraries/agent-coaching]] · [[../specs/board-grooming]] · [[../libraries/platform-director]] · [[director_messages]] · [[../libraries/director-board]] · [[../libraries/deploy-guardian]] · [[deploy_watches]] · [[../specs/deploy-health-rollback-guardian]] · [[../specs/needs-attention-triage-and-verdict-robustness]]
